services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: drp
      POSTGRES_PASSWORD: drp
      POSTGRES_DB: drp
    ports:
      - "5432:5432"
    volumes:
      - drp_pgdata:/var/lib/postgresql/data

volumes:
  drp_pgdata:
