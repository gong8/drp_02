# Refactor report - 2026-06-09

> Generated by `/simplify-refactor everything apply + aggressive`. A massively-parallel, multi-lens
> code-quality pass (186 agents) over the whole repo excluding `archive/`, build output, generated
> migrations, and tests. Every cluster below survived an adversarial skeptic that re-read the files and
> tried to refute it. All changes are behavior-preserving; no feature work, no functional bug fixes.

## Executive summary

- **158** raw findings from **91** lens reviewers + **6** cross-cutting sweeps.
- Triaged to **56** candidate clusters; **32** survived adversarial verification (24 dropped as false positives, trivia, or convention-fighting).
- Every survivor is **S effort** and **low risk** (one med-risk item flagged for scrutiny). Theme: extract a single source of truth for logic/strings/types that had drifted into 2-5 hand-copied sites, and reuse the existing `ui/` + `lib/` + `@bethere/shared` surface instead of re-inlining.
- The mobile/shared type-only boundary was respected throughout: no mobile helper is merged into the shared package via a runtime import.

## Prioritized roadmap

| # | Cluster | Effort | Risk | Impact | Files |
|---|---------|:------:|:----:|:------:|-------|
| 1 | **Add a fallbackUserCard helper in db/users.ts and route both routers through it** | S | low | low | apps/api/src/db/users.ts<br>apps/api/src/routers/groups.ts<br>apps/api/src/routers/events.ts |
| 2 | **Reuse shared CandidateReaction instead of re-spelling the inline {candidateId,userId} type 5x** | S | low | low | apps/api/src/routers/events.ts |
| 3 | **Extract the toggleReaction (eventId,candidateId,userId) reaction predicate built twice** | S | low | low | apps/api/src/routers/events.ts |
| 4 | **Share the 'clear my response' delete between respond and unrespond** | S | low | low | apps/api/src/routers/events.ts |
| 5 | **Unify addCandidate with create's candidate insert + minute/activity dedupe helpers** | S | low | med | apps/api/src/routers/events.ts |
| 6 | **Add a shared isTerminalPhase helper for the cleared/fizzled pair on the API side** | S | low | low | packages/shared/src/logic/resolve.ts<br>apps/api/src/routers/events.ts<br>apps/api/src/db/seed.ts |
| 7 | **Factor the Map group-by idiom in loadEventBundle and the per-table member-purge in removeMember** | S | low | low | apps/api/src/routers/events.ts<br>apps/api/src/routers/groups.ts |
| 8 | **Consolidate group reads: reuse getGroupNames, add memberCount helper, drop dead getGroupName** | S | low | low | apps/api/src/routers/groups.ts<br>apps/api/src/db/groups.ts |
| 9 | **Share memberIdsOf so events.ts stops re-inlining the member-id query** | S | low | low | apps/api/src/db/groups.ts<br>apps/api/src/routers/groups.ts<br>apps/api/src/routers/events.ts |
| 10 | **Drive pgEnum value tuples from the shared z.enum single source of truth** | S | low | med | apps/api/src/db/schema.ts |
| 11 | **Parameterize the repeated FieldEdit.refine(max, message) in UpdateEventInput** | S | low | low | packages/shared/src/schemas.ts |
| 12 | **Reuse the shared Conditional type and ActivityText rule instead of re-spelling them** | S | low | low | packages/shared/src/schemas.ts<br>packages/shared/src/logic/resolve.ts<br>apps/api/src/db/schema.ts |
| 13 | **Consume activeDeadline's label in EventDetail's CountdownBanner instead of re-hardcoding the phase->label mapping** | S | low | low | apps/mobile/src/screens/EventDetail.tsx |
| 14 | **Rename MomentView's editing/onEdit which collide with the edit-details vocabulary** | S | low | low | apps/mobile/src/screens/EventDetail.tsx<br>apps/mobile/src/lib/useBusyAction.ts |
| 15 | **De-duplicate the What/When confirm-step SummaryItem note logic in CreateWizard** | S | low | low | apps/mobile/src/screens/CreateWizard.tsx |
| 16 | **Express isConcrete from timeFixed/activityFixed and factor the reply-close deadline line** | S | low | low | apps/mobile/src/screens/CreateWizard.tsx |
| 17 | **Use the shared DEADLINE_VOTING constant in CreateWizard's confirm preview** | S | low | low | apps/mobile/src/screens/CreateWizard.tsx |
| 18 | **Route Dashboard no-groups title + CTA through copy.ts** | S | low | low | apps/mobile/src/screens/Dashboard.tsx |
| 19 | **Have compareActions reuse activeDeadline's phase-to-field mapping** | S | low | low | apps/mobile/src/lib/status.ts |
| 20 | **Extract a one-shot route-param consume hook in GroupDetail** | S | low | low | apps/mobile/src/screens/GroupDetail.tsx |
| 21 | **Extract a shared bordered input-frame style constant for Field, DateTimeField, and DateTimePill** | S | low | low | apps/mobile/src/theme.ts<br>apps/mobile/src/ui/Field.tsx<br>apps/mobile/src/ui/DateTimeField.tsx<br>apps/mobile/src/ui/DateTimePill.tsx |
| 22 | **De-duplicate DateTimeField's mode-gated picker props across Android/iOS branches** | S | low | low | apps/mobile/src/ui/DateTimeField.tsx |
| 23 | **Add a one-letter avatar-initial helper for person avatars** | S | low | low | apps/mobile/src/lib/format.ts<br>apps/mobile/src/ui/PersonRow.tsx<br>apps/mobile/src/screens/Account.tsx<br>apps/mobile/src/components/AccountAvatar.tsx |
| 24 | **Replace the inlined display-font-15 heading recipe with an AppText variant** | S | low | low | apps/mobile/src/ui/Text.tsx<br>apps/mobile/src/screens/GroupsList.tsx<br>apps/mobile/src/screens/EventDetail.tsx |
| 25 | **Add a paragraph-caption variant instead of the scattered lineHeight:18 override** | S | low | low | apps/mobile/src/ui/Text.tsx<br>apps/mobile/src/screens/Dashboard.tsx<br>apps/mobile/src/screens/EventDetail.tsx<br>apps/mobile/src/screens/GroupsList.tsx<br>apps/mobile/src/screens/JoinGroup.tsx<br>apps/mobile/src/ui/Section.tsx |
| 26 | **Extract the tap-to-Account avatar header slot duplicated in GroupsList and Dashboard** | S | low | low | apps/mobile/src/components/AccountHeaderButton.tsx<br>apps/mobile/src/screens/GroupsList.tsx<br>apps/mobile/src/screens/Dashboard.tsx |
| 27 | **Have GroupsList use the shared useFetchOnFocus hook** | S | med | low | apps/mobile/src/screens/GroupsList.tsx |
| 28 | **Use the ui.gutter token instead of the hardcoded 16 in ScreenBackground** | S | low | low | apps/mobile/src/ui/ScreenBackground.tsx |
| 29 | **Collapse the dead dismissedAction branch in the native share path** | S | low | low | apps/mobile/src/lib/share.ts |
| 30 | **Derive the wizard STEPS array from the StepKey union (single-source the step order)** | S | low | low | apps/mobile/src/lib/redo.ts |
| 31 | **Share the time-candidate start-extraction (filter + as-Date cast) across seed modules** | S | low | low | apps/api/src/db/seed-data.ts<br>apps/api/src/db/seed.ts |
| 32 | **Share the onPress ? Pressable : View wrapper across Card, Row, PersonRow** | S | low | low | apps/mobile/src/ui/Tappable.tsx<br>apps/mobile/src/ui/Card.tsx<br>apps/mobile/src/ui/Row.tsx<br>apps/mobile/src/ui/PersonRow.tsx<br>apps/mobile/src/ui/index.ts |

---

## 1. Add a fallbackUserCard helper in db/users.ts and route both routers through it

`api-fallback-user-card-helper` - **effort** S | **risk** low | **impact** low

### Problem
The missing-row user-card sentinel literal `map.get(id) ?? { id, name: FALLBACK_USER_NAME, color: FALLBACK_AVATAR_COLOR }` is hand-copied at groups.ts:89 and events.ts:449. db/users.ts already owns the UserCard type, both FALLBACK_* sentinels, and getUserCards, and its comment states the card shape is named once so a field add/rename touches one place - but there is no helper that resolves a card FROM A MAP with the fallback, so the construction site is duplicated in two routers. Add one exported pure helper `fallbackUserCard(id)` in db/users.ts beside getUserCards, then replace both inline literals with `map.get(id) ?? fallbackUserCard(id)`. Each router drops its now-unused FALLBACK_USER_NAME/FALLBACK_AVATAR_COLOR imports (verified used nowhere else in those two files) and imports fallbackUserCard instead. Behavior-preserving: identical object shape and sentinel values. getUserCard (users.ts:23-30) is intentionally left as-is because its per-field merge off a partial row is a different operation from the map fallback.

### Why it matters
db/users.ts deliberately single-sources the UserCard shape and the two FALLBACK_* sentinels (its own comment says a field add/rename should touch one definition), yet the map-fallback construction is hand-copied in two routers, so a future field add to UserCard would silently miss those two sites. One pure helper closes that gap and shrinks each router's import surface, with zero behavior change.

### Change steps
- In apps/api/src/db/users.ts, add an exported pure helper right after getUserCards (around line 42): `export function fallbackUserCard(id: string): UserCard { return { id, name: FALLBACK_USER_NAME, color: FALLBACK_AVATAR_COLOR }; }`.
- In apps/api/src/routers/groups.ts, replace the inline literal at line 89 `cardMap.get(id) ?? { id, name: FALLBACK_USER_NAME, color: FALLBACK_AVATAR_COLOR }` with `cardMap.get(id) ?? fallbackUserCard(id)`.
- In apps/api/src/routers/groups.ts, update the db/users.js import block (lines 24-29): drop FALLBACK_AVATAR_COLOR and FALLBACK_USER_NAME, add fallbackUserCard (keep getUserCards and userCardFromRow).
- In apps/api/src/routers/events.ts, replace the inline literal at line 449 `userCardMap.get(id) ?? { id, name: FALLBACK_USER_NAME, color: FALLBACK_AVATAR_COLOR }` with `userCardMap.get(id) ?? fallbackUserCard(id)`.
- In apps/api/src/routers/events.ts, update the value import at line 39 `import { FALLBACK_AVATAR_COLOR, FALLBACK_USER_NAME, getUserCards } from "../db/users.js";` to `import { fallbackUserCard, getUserCards } from "../db/users.js";` (the type-only `import type { UserCard }` on line 38 stays).
- Do NOT modify getUserCard (users.ts:23-30): its per-field merge off a partial users row is a different operation and must stay to preserve behavior.
- Run pnpm typecheck, pnpm lint, and pnpm test to confirm no behavior or import drift.

### Before / after
```ts
// apps/api/src/db/users.ts - add beside getUserCards
+ // Card for a member id with no users row (deleted/never-seeded), built from the sentinels so the
+ // missing-row shape lives in one place that callers resolving a getUserCards map can reach for.
+ export function fallbackUserCard(id: string): UserCard {
+   return { id, name: FALLBACK_USER_NAME, color: FALLBACK_AVATAR_COLOR };
+ }

// apps/api/src/routers/groups.ts
- import { FALLBACK_AVATAR_COLOR, FALLBACK_USER_NAME, getUserCards, userCardFromRow } from "../db/users.js";
+ import { fallbackUserCard, getUserCards, userCardFromRow } from "../db/users.js";
  ...
- const members = ids.map(
-   (id) => cardMap.get(id) ?? { id, name: FALLBACK_USER_NAME, color: FALLBACK_AVATAR_COLOR },
- );
+ const members = ids.map((id) => cardMap.get(id) ?? fallbackUserCard(id));

// apps/api/src/routers/events.ts
- import { FALLBACK_AVATAR_COLOR, FALLBACK_USER_NAME, getUserCards } from "../db/users.js";
+ import { fallbackUserCard, getUserCards } from "../db/users.js";
  ...
- userCard: (id) =>
-   userCardMap.get(id) ?? { id, name: FALLBACK_USER_NAME, color: FALLBACK_AVATAR_COLOR },
+ userCard: (id) => userCardMap.get(id) ?? fallbackUserCard(id),
```

### Files
- `apps/api/src/db/users.ts`
- `apps/api/src/routers/groups.ts`
- `apps/api/src/routers/events.ts`

---

## 2. Reuse shared CandidateReaction instead of re-spelling the inline {candidateId,userId} type 5x

`reuse-shared-candidate-reaction-type` - **effort** S | **risk** low | **impact** low

### Problem
In apps/api/src/routers/events.ts the inline reaction type `{ candidateId: string; userId: string }[]` is hand-spelled five times (reactionsFor return at :70, isReadyToLock param at :96, openMoment param at :203, loadEventBundle accessor at :379, reactMap value at :418). @bethere/shared already exports that exact shape as the named interface CandidateReaction (packages/shared/src/logic/candidates.ts:2-5), and is used by pickWinningCandidate/pickWinnerOrBestId/tallyCandidates in that same module - the very functions events.ts passes these arrays into. events.ts already imports from the @bethere/shared barrel, so this is a pure type-level reuse: add `type CandidateReaction` to the existing import and replace the five literals with `CandidateReaction[]`. Optionally annotate the toReaction mapper (:60-63) as `: CandidateReaction` to pin it to the shared shape. No runtime change; verify with pnpm typecheck.

### Why it matters
CandidateReaction is the canonical shape for these arrays - they are tallied by pickWinningCandidate/pickWinnerOrBestId in the same shared module, which already type their reactions param as CandidateReaction[]. Re-spelling the literal 5 times in events.ts is duplicated structural typing that the shared barrel already names; the inline copies can silently drift from the source of truth if the interface ever gains a field. Using the named type makes the consumer/producer contract explicit and self-documenting at zero runtime cost (this is the API side, so a runtime import of @bethere/shared is fine - the mobile type-only constraint does not apply).

### Change steps
- Add `type CandidateReaction` to the existing `import { ... } from "@bethere/shared"` block at the top of apps/api/src/routers/events.ts (lines 2-25)
- Line 70: change `reactionsFor`'s return from `Promise<{ candidateId: string; userId: string }[]>` to `Promise<CandidateReaction[]>`
- Line 96: change `isReadyToLock`'s `reactions` param from `{ candidateId: string; userId: string }[]` to `CandidateReaction[]`
- Line 203: change `openMoment`'s `reactions` param from `{ candidateId: string; userId: string }[]` to `CandidateReaction[]`
- Line 379: change `loadEventBundle`'s `reactions` accessor return from `(id: string) => { candidateId: string; userId: string }[]` to `(id: string) => CandidateReaction[]`
- Line 418: change `reactMap` from `Map<string, { candidateId: string; userId: string }[]>` to `Map<string, CandidateReaction[]>`
- Optional: annotate the `toReaction` mapper (lines 60-63) return type as `: CandidateReaction` to pin the mapper to the shared shape
- Run `pnpm typecheck` to confirm the type-only change compiles cleanly

### Before / after
```ts
// apps/api/src/routers/events.ts

// import block (lines 2-25) - add the named type alongside the existing imports
import {
  AddCandidateInput,
  // ...existing imports...
  type CandidateKind,
  type CandidateReaction,   // <-- add
  // ...
} from "@bethere/shared";

// BEFORE
const toReaction = (r: typeof candidateReactions.$inferSelect) => ({
  candidateId: r.candidateId,
  userId: r.userId,
});

async function reactionsFor(eventId: string): Promise<{ candidateId: string; userId: string }[]> { ... }

function isReadyToLock(
  timeIds: string[],
  reactions: { candidateId: string; userId: string }[],
  quorum: number,
): boolean { ... }

async function openMoment(
  e: EventRow,
  chosen: { startsAt: Date | null },
  chosenId: string,
  cands: (typeof eventCandidates.$inferSelect)[],
  reactions: { candidateId: string; userId: string }[],
): Promise<boolean> { ... }

async function loadEventBundle(...): Promise<{
  // ...
  reactions: (id: string) => { candidateId: string; userId: string }[];
  // ...
}> { ... }

const reactMap = new Map<string, { candidateId: string; userId: string }[]>();

// AFTER
const toReaction = (r: typeof candidateReactions.$inferSelect): CandidateReaction => ({
  candidateId: r.candidateId,
  userId: r.userId,
});

async function reactionsFor(eventId: string): Promise<CandidateReaction[]> { ... }

function isReadyToLock(
  timeIds: string[],
  reactions: CandidateReaction[],
  quorum: number,
): boolean { ... }

async function openMoment(
  e: EventRow,
  chosen: { startsAt: Date | null },
  chosenId: string,
  cands: (typeof eventCandidates.$inferSelect)[],
  reactions: CandidateReaction[],
): Promise<boolean> { ... }

async function loadEventBundle(...): Promise<{
  // ...
  reactions: (id: string) => CandidateReaction[];
  // ...
}> { ... }

const reactMap = new Map<string, CandidateReaction[]>();
```

### Files
- `apps/api/src/routers/events.ts`

---

## 3. Extract the toggleReaction (eventId,candidateId,userId) reaction predicate built twice

`extract-togglereaction-reaction-predicate` - **effort** S | **risk** low | **impact** low

### Problem
In `toggleReaction` (apps/api/src/routers/events.ts), the identical three-clause row-identity predicate `and(eq(candidateReactions.eventId, input.eventId), eq(candidateReactions.candidateId, input.candidateId), eq(candidateReactions.userId, ctx.userId))` is written verbatim twice: once for the existence SELECT `.where()` (lines 643-649) and again for the DELETE `.where()` (lines 653-659). Lift it into a single local const so the (eventId, candidateId, userId) reaction-row identity lives in exactly one place and both queries reuse it. Pure de-duplication: the generated SQL is identical, just one expression instead of two copies. Do NOT touch the distinct two-clause setOptOut predicate (lines 684-689), which intentionally omits candidateId.

### Why it matters
The row-identity predicate for a single candidateReactions row is duplicated verbatim across the read-then-delete pair in one function. Hoisting it removes the risk that a future edit changes one copy and not the other (e.g. adding a column to the identity), and makes the read and delete provably target the same row.

### Change steps
- In apps/api/src/routers/events.ts, inside toggleReaction, immediately after the candidate-existence guard (after line 638, before the SELECT at line 640), add one local const: `const mineWhere = and(eq(candidateReactions.eventId, input.eventId), eq(candidateReactions.candidateId, input.candidateId), eq(candidateReactions.userId, ctx.userId));`
- Replace the inline three-clause `and(...)` argument of the existence SELECT `.where(...)` (lines 643-649) with `.where(mineWhere)`.
- Replace the inline three-clause `and(...)` argument of the DELETE `.where(...)` (lines 653-659) with `.where(mineWhere)`.
- Leave the two-clause setOptOut delete predicate (lines 684-689) untouched - it omits candidateId and is a different predicate.
- No import change needed: `and` and `eq` are already imported from drizzle-orm (line 27).
- Run pnpm lint, pnpm typecheck, and pnpm test to confirm no behavior change.

### Before / after
```ts
BEFORE (apps/api/src/routers/events.ts, toggleReaction):

    const mine = await db
      .select()
      .from(candidateReactions)
      .where(
        and(
          eq(candidateReactions.eventId, input.eventId),
          eq(candidateReactions.candidateId, input.candidateId),
          eq(candidateReactions.userId, ctx.userId),
        ),
      );
    if (mine.length > 0) {
      await db
        .delete(candidateReactions)
        .where(
          and(
            eq(candidateReactions.eventId, input.eventId),
            eq(candidateReactions.candidateId, input.candidateId),
            eq(candidateReactions.userId, ctx.userId),
          ),
        );
      return { reacted: false as const };
    }

AFTER:

    const mineWhere = and(
      eq(candidateReactions.eventId, input.eventId),
      eq(candidateReactions.candidateId, input.candidateId),
      eq(candidateReactions.userId, ctx.userId),
    );
    const mine = await db.select().from(candidateReactions).where(mineWhere);
    if (mine.length > 0) {
      await db.delete(candidateReactions).where(mineWhere);
      return { reacted: false as const };
    }

(setOptOut's two-clause predicate at lines 684-689 stays as-is - it intentionally omits candidateId.)
```

### Files
- `apps/api/src/routers/events.ts`

---

## 4. Share the 'clear my response' delete between respond and unrespond

`share-clear-my-response-delete` - **effort** S | **risk** low | **impact** low

### Problem
`respond` (events.ts:1080-1082) and `unrespond` (events.ts:1100-1102) run the identical "delete this user's RSVP row" query: `db.delete(responses).where(and(eq(responses.eventId, ...), eq(responses.userId, ...)))`. The same file already factors the analogous opt-out delete into a `clearOptOut(eventId, userId, handle: DbOrTx = db)` helper (events.ts:173-177). Add a sibling `clearMyResponse` helper right next to it and call it from both mutations. Pure behavior-preserving dedup: removes the copied predicate and gives one source for "drop this user's RSVP row". Verified that these are the only two `delete(responses)` sites and both are byte-for-byte identical; both call-sites use the default `db` handle (neither mutation wraps the delete in a transaction), so the optional `handle` param is for symmetry with `clearOptOut` only.

### Why it matters
Removes a copy-pasted query predicate and establishes a single source of truth for "drop this user's RSVP row", consistent with the existing clearOptOut helper. If the responses match ever changes, it now changes in one place.

### Change steps
- Add `clearMyResponse(eventId: string, userId: string, handle: DbOrTx = db): Promise<void>` directly below `clearOptOut` (~events.ts:177), body `await handle.delete(responses).where(and(eq(responses.eventId, eventId), eq(responses.userId, userId)))`, mirroring clearOptOut's DbOrTx default-handle signature and one-line doc comment.
- In `respond` (events.ts:1080-1082) replace the inline `await db.delete(responses).where(...)` with `await clearMyResponse(input.eventId, ctx.userId)`, keeping it before the subsequent `db.insert(responses)`.
- In `unrespond` (events.ts:1100-1102) replace the inline `await db.delete(responses).where(...)` with `await clearMyResponse(input.eventId, ctx.userId)`.
- Run `pnpm lint`, `pnpm typecheck`, `pnpm test` (API tests cover respond/unrespond) to confirm no behavior change.

### Before / after
```ts
// Before - identical block in two places (respond ~1080, unrespond ~1100):
await db
  .delete(responses)
  .where(and(eq(responses.eventId, input.eventId), eq(responses.userId, ctx.userId)));

// After - one helper beside clearOptOut (~events.ts:177):
// Drop the caller's RSVP row (the "Change" action returns them to un-answered; respond replaces it
// before inserting the new answer). Idempotent; pass a tx handle to enlist in a wrapping transaction.
async function clearMyResponse(eventId: string, userId: string, handle: DbOrTx = db): Promise<void> {
  await handle
    .delete(responses)
    .where(and(eq(responses.eventId, eventId), eq(responses.userId, userId)));
}

// ...and both call-sites collapse to:
await clearMyResponse(input.eventId, ctx.userId);
```

### Files
- `apps/api/src/routers/events.ts`

---

## 5. Unify addCandidate with create's candidate insert + minute/activity dedupe helpers

`api-events-unify-addcandidate-insert-and-dedupe-helpers` - **effort** S | **risk** low | **impact** med

### Problem
In apps/api/src/routers/events.ts, the addCandidate mutation re-implements logic that create already centralises. It spells out the single-row eventCandidates insert twice (the time branch at lines 758-765 and the activity branch at lines 781-788) instead of reusing the existing insertCandidates helper (defined at line 137, already used by create at line 624). The minute-bucket time dedupe `Math.floor(d.getTime() / 60_000)` is inlined in three spots with the same magic 60_000 and twin "mirrors addCandidate"/"mirrors create" comments: create's collapse filter (line 491) and addCandidate's new-slot minute plus the dup predicate (lines 746 and 751). And the activity case-insensitive trim+lowercase dedupe key is computed independently in create (lines 502/505) and addCandidate (lines 770/772 for the new text, line 774 for the existing row) with comments that assert the two paths must mirror each other. Extract two module-local pure helpers - a minute-bucket and an activity-key normalizer - and route addCandidate's single new row through insertCandidates, so the granularity and key rules live in exactly one place. All three changes are behavior-preserving: insertCandidates writes the same column set the inline blocks do, the minute helper is the identical expression, and the activity key is idempotent under repeated trimming.

### Why it matters
The three duplications encode invariants that the existing comments already assert must stay in lockstep ("mirrors addCandidate's minute dedupe", "mirroring addCandidate's key"): the create and add paths must bucket times at the same granularity and normalize activity names the same way, or a slot/name added one way splits +1 momentum from the same slot/name added the other way and can quietly break the both-axes-pinned moment shortcut (planOpensMoment needs exactly one time candidate). Today those rules live in 2-3 copies each, so a future change to granularity or normalization has to be made in every copy or the paths silently diverge. Extracting one minute helper, one activity-key helper, and reusing the already-present insertCandidates collapses the magic 60_000 and the trim/lowercase rule to a single definition each and routes both create and addCandidate through the same insert column set - guaranteeing the mirror the comments promise, with zero behavior change.

### Change steps
- Add a module-local `const MINUTE_MS = 60_000;` and `const startMinute = (d: Date) => Math.floor(d.getTime() / MINUTE_MS);` near the top of apps/api/src/routers/events.ts (alongside insertCandidates/ensureReaction).
- Add a module-local `const activityKey = (s: string) => s.trim().toLowerCase();`.
- In create's minute-collapse filter (line 490-491), replace `Math.floor(c.startsAt.getTime() / 60_000)` with `startMinute(c.startsAt)`; keep the explanatory comment, now pointing at the shared helper.
- In create's activity dedupe (lines 501-509), use `activityKey(text)` for the dedupe key in place of `text.toLowerCase()` on the already-trimmed text (idempotent: the text is trimmed at line 502, so trimming again is a no-op).
- In addCandidate's time branch, replace `const startMinute = Math.floor(startsAt.getTime() / 60_000);` (line 746) with `const minute = startMinute(startsAt);` and the dup predicate's `Math.floor(c.startsAt.getTime() / 60_000) === startMinute` (line 751) with `startMinute(c.startsAt) === minute`. (Rename the local to avoid shadowing the new helper.)
- In addCandidate's activity branch, replace `const key = text.toLowerCase();` (line 772) with `const key = activityKey(text);` and the existing-row key `(c.label ?? "").trim().toLowerCase()` (line 774) with `activityKey(c.label ?? "")`.
- Collapse addCandidate's two `db.insert(eventCandidates).values({...})` blocks (lines 758-765 and 781-788): in each branch set `newId` and the kind-specific fields (startsAt/partOfDay/label), then after the if/else call a single `await insertCandidates(input.eventId, [{ id: newId, kind: input.kind, startsAt, partOfDay, label }]);`, followed by the existing `await ensureReaction(input.eventId, newId, ctx.userId);` at line 790.
- Run `pnpm --filter @bethere/api typecheck` (or `pnpm typecheck`) and the events-*.test.ts suite to confirm no behavior change.

### Before / after
```ts
// apps/api/src/routers/events.ts - module-local helpers (near insertCandidates, line ~137)
// BEFORE: 60_000 inlined in 3 spots; trim+lowercase key computed in 4 spots; two inline inserts.
// AFTER:
const MINUTE_MS = 60_000;
const startMinute = (d: Date) => Math.floor(d.getTime() / MINUTE_MS);
const activityKey = (s: string) => s.trim().toLowerCase();

// --- create (collecting-list build) ---
// minute collapse (was: Math.floor(c.startsAt.getTime() / 60_000))
const minute = startMinute(c.startsAt);
// activity dedupe key (was: const key = text.toLowerCase())
const key = activityKey(text);

// --- addCandidate, time branch ---
// BEFORE
//   const startMinute = Math.floor(startsAt.getTime() / 60_000);
//   const dup = cands.find((c) => c.kind === "time" && c.startsAt != null &&
//     Math.floor(c.startsAt.getTime() / 60_000) === startMinute);
//   ...
//   newId = `${input.eventId}_t_${randomUUID()}`;
//   await db.insert(eventCandidates).values({ id: newId, eventId: input.eventId,
//     kind: "time", startsAt, partOfDay: input.partOfDay ?? null, label: null });
// AFTER (branch sets shared locals, no insert here)
const minute = startMinute(startsAt);
const dup = cands.find((c) => c.kind === "time" && c.startsAt != null &&
  startMinute(c.startsAt) === minute);
if (dup) { await ensureReaction(input.eventId, dup.id, ctx.userId); return { id: dup.id }; }
newId = `${input.eventId}_t_${randomUUID()}`;
partOfDay = input.partOfDay ?? null;  // label stays null

// --- addCandidate, activity branch ---
// BEFORE
//   const key = text.toLowerCase();
//   const dup = cands.find((c) => c.kind === "activity" &&
//     (c.label ?? "").trim().toLowerCase() === key);
//   ...
//   await db.insert(eventCandidates).values({ id: newId, eventId: input.eventId,
//     kind: "activity", startsAt: null, partOfDay: null, label: text });
// AFTER
const key = activityKey(text);
const dup = cands.find((c) => c.kind === "activity" && activityKey(c.label ?? "") === key);
if (dup) { await ensureReaction(input.eventId, dup.id, ctx.userId); return { id: dup.id }; }
newId = `${input.eventId}_a_${randomUUID()}`;
label = text;  // startsAt/partOfDay stay null

// --- single shared insert after the if/else (replaces both inline inserts) ---
await insertCandidates(input.eventId, [
  { id: newId, kind: input.kind, startsAt, partOfDay, label },
]);
await ensureReaction(input.eventId, newId, ctx.userId);
return { id: newId };
```

### Files
- `apps/api/src/routers/events.ts`

---

## 6. Add a shared isTerminalPhase helper for the cleared/fizzled pair on the API side

`api-isterminalphase-helper` - **effort** S | **risk** low | **impact** low

### Problem
The terminal-phase rule `phase === "cleared" || phase === "fizzled"` is hand-spelled in three API/shared-side call sites - events.ts:111, events.ts:838, and seed.ts:54 - with no shared helper (the mobile client has lib/status.isTerminal, but it takes an object and never crosses the type-only boundary into the API). Add one tiny runtime helper, `isTerminalPhase(phase: PlanPhase): boolean`, in packages/shared/src/logic (re-exported by the shared barrel, so both the API router and the DB seed can import it at runtime) and call it at the three sites. Behavior-identical. The DAY_MS finding is dropped per verification: DAY_MS is exported by @bethere/shared but is NOT currently imported into events.ts (only MOMENT_MS is), so swapping `7 * 24 * 60 * 60 * 1000` for `7 * DAY_MS` would mean adding a new import for a single-call-site cosmetic tweak - below the bar. seed-data.ts:284's needsChosen (`moment || cleared || fizzled`) is a different three-phase set and stays out of scope.

### Why it matters
Removes a duplicated terminal-phase predicate from three API/shared-side sites and gives the API the same single source of truth the mobile client already has, so the cleared/fizzled rule can never drift between the router and the seed.

### Change steps
- Add `export function isTerminalPhase(phase: PlanPhase): boolean { return phase === "cleared" || phase === "fizzled"; }` to packages/shared/src/logic/resolve.ts, importing the PlanPhase type from ../schemas.js (the file already deals with phase transitions/clears, and the barrel index.ts already re-exports ./logic/resolve.js so it becomes importable from @bethere/shared with no barrel edit).
- In apps/api/src/routers/events.ts, add `isTerminalPhase` to the existing `@bethere/shared` import block (alphabetically near MOMENT_MS / pickWinnerOrBestId).
- events.ts:111 - in goingFromRow, replace `terminal: e.phase === "cleared" || e.phase === "fizzled",` with `terminal: isTerminalPhase(e.phase),`.
- events.ts:838 - in the update mutation guard, replace `if (e.phase === "cleared" || e.phase === "fizzled") {` with `if (isTerminalPhase(e.phase)) {`.
- In apps/api/src/db/seed.ts, import `isTerminalPhase` from `@bethere/shared`, then at seed.ts:54 replace `status: p.phase === "cleared" || p.phase === "fizzled" ? "resolved" : "open",` with `status: isTerminalPhase(p.phase) ? "resolved" : "open",`.
- Do NOT touch seed-data.ts:284 (needsChosen = moment||cleared||fizzled, a different set). Do NOT add a DAY_MS import or change events.ts:51 (dropped per verification).
- Run `pnpm typecheck`, `pnpm lint`, and `pnpm test` to confirm behavior is unchanged.

### Before / after
```ts
// packages/shared/src/logic/resolve.ts  (NEW helper; barrel already re-exports this file)
import type { PlanPhase } from "../schemas.js";
// A plan is terminal once it has cleared or fizzled.
export function isTerminalPhase(phase: PlanPhase): boolean {
  return phase === "cleared" || phase === "fizzled";
}

// apps/api/src/routers/events.ts
import { /* ...MOMENT_MS, */ isTerminalPhase, /* ... */ } from "@bethere/shared";
// :111  before -> terminal: e.phase === "cleared" || e.phase === "fizzled",
//        after  -> terminal: isTerminalPhase(e.phase),
// :838  before -> if (e.phase === "cleared" || e.phase === "fizzled") {
//        after  -> if (isTerminalPhase(e.phase)) {

// apps/api/src/db/seed.ts
import { isTerminalPhase } from "@bethere/shared";
// :54   before -> status: p.phase === "cleared" || p.phase === "fizzled" ? "resolved" : "open",
//        after  -> status: isTerminalPhase(p.phase) ? "resolved" : "open",

// UNCHANGED: events.ts:51 (no DAY_MS import - dropped), seed-data.ts:284 needsChosen (3-phase set)
```

### Files
- `packages/shared/src/logic/resolve.ts`
- `apps/api/src/routers/events.ts`
- `apps/api/src/db/seed.ts`

---

## 7. Factor the Map group-by idiom in loadEventBundle and the per-table member-purge in removeMember

`api-groupby-and-member-purge-dedup` - **effort** S | **risk** low | **impact** low

### Problem
Two same-file repeated idioms in the API, both behavior-preserving extractions. (1) In apps/api/src/routers/events.ts, loadEventBundle hand-rolls the identical "get-or-[], push, set" list-map append loop three times (respMap 402-407, candMap 408-413, reactMap 418-423), differing only in the per-row mapper; extract a local generic groupBy helper and rebuild the three maps through it. (2) In apps/api/src/routers/groups.ts, removeMember purges the leaving user's votes with three copy-pasted delete(table).where(and(eq(table.userId,...), inArray(table.eventId, eventIds))) blocks across candidateReactions/responses/eventOptOuts (lines 200-217); loop over the table list (optionally via a deleteUserRows helper) so the predicate is written once. Verified: the three group-by loops differ only by mapper (toMomentResponse line 55, toReaction line 60, identity for candidates); candMap's extra sort pass (byStartsAtThenNullsLast, 414-417) and optMap's Set build (424-429) stay separate; all three purge tables share eventId+userId columns (schema.ts) so the loop predicate type-checks, though the heterogeneous loop variable may need an explicit annotation.

### Why it matters
Removes three byte-identical group-by append loops and three copy-pasted delete predicates within two files. Each idiom's intent (group rows by eventId; purge a leaving user's votes across vote tables) becomes one named operation, so a future column/table change is edited once instead of three times, and the candMap sort and optMap Set special-cases stand out as the genuinely different paths. Pure dedup: no schema, query, or response-shape change.

### Change steps
- events.ts: add a local generic helper `function groupBy<T, K, V>(rows: readonly T[], keyOf: (r: T) => K, valueOf: (r: T) => V): Map<K, V[]>` near the existing row-mapper helpers (after byStartsAtThenNullsLast, ~line 80).
- events.ts: rebuild respMap via `groupBy(respRows, (r) => r.eventId, toMomentResponse)`, candMap via `groupBy(candRows, (c) => c.eventId, (c) => c)`, reactMap via `groupBy(reactRows, (r) => r.eventId, toReaction)`; delete the three manual get-or-init loops (402-407, 408-413, 418-423).
- events.ts: keep candMap's `for (const list of candMap.values()) list.sort(byStartsAtThenNullsLast)` pass (414-417) unchanged, immediately after the groupBy call that builds candMap.
- events.ts: leave optMap's Set-valued loop (424-429) as-is (it is not a list map); optionally add a `groupBySet` sibling, but prefer leaving it untouched to keep the diff minimal.
- groups.ts: in removeMember, replace the three delete blocks (200-217) with a loop over the table list, e.g. `for (const t of [candidateReactions, responses, eventOptOuts] as const) await tx.delete(t).where(and(eq(t.userId, input.userId), inArray(t.eventId, eventIds)));`, or factor a local `deleteUserRows(tx, t, userId, eventIds)` helper invoked once per table.
- groups.ts: keep the `if (eventIds.length > 0)` guard around the loop unchanged.
- Run pnpm typecheck (the heterogeneous table loop variable may need an explicit type annotation), then pnpm lint and pnpm test.

### Before / after
```ts
events.ts (loadEventBundle)
// before - 3x of this, differing only in the mapper:
const respMap = new Map<string, MomentResponse[]>();
for (const r of respRows) {
  const list = respMap.get(r.eventId) ?? [];
  list.push(toMomentResponse(r));
  respMap.set(r.eventId, list);
}
const candMap = new Map<string, (typeof eventCandidates.$inferSelect)[]>();
for (const c of candRows) { const list = candMap.get(c.eventId) ?? []; list.push(c); candMap.set(c.eventId, list); }
const reactMap = new Map<string, { candidateId: string; userId: string }[]>();
for (const r of reactRows) { const list = reactMap.get(r.eventId) ?? []; list.push(toReaction(r)); reactMap.set(r.eventId, list); }

// after
function groupBy<T, K, V>(rows: readonly T[], keyOf: (r: T) => K, valueOf: (r: T) => V): Map<K, V[]> {
  const m = new Map<K, V[]>();
  for (const r of rows) {
    const k = keyOf(r);
    const list = m.get(k) ?? [];
    list.push(valueOf(r));
    m.set(k, list);
  }
  return m;
}
const respMap = groupBy(respRows, (r) => r.eventId, toMomentResponse);
const candMap = groupBy(candRows, (c) => c.eventId, (c) => c);
for (const list of candMap.values()) list.sort(byStartsAtThenNullsLast); // unchanged sort pass
const reactMap = groupBy(reactRows, (r) => r.eventId, toReaction);
// optMap Set loop left as-is

groups.ts (removeMember)
// before - 3 copy-pasted blocks
await tx.delete(candidateReactions).where(and(eq(candidateReactions.userId, input.userId), inArray(candidateReactions.eventId, eventIds)));
await tx.delete(responses).where(and(eq(responses.userId, input.userId), inArray(responses.eventId, eventIds)));
await tx.delete(eventOptOuts).where(and(eq(eventOptOuts.userId, input.userId), inArray(eventOptOuts.eventId, eventIds)));
// after
for (const t of [candidateReactions, responses, eventOptOuts] as const) {
  await tx.delete(t).where(and(eq(t.userId, input.userId), inArray(t.eventId, eventIds)));
}
```

### Files
- `apps/api/src/routers/events.ts`
- `apps/api/src/routers/groups.ts`

---

## 8. Consolidate group reads: reuse getGroupNames, add memberCount helper, drop dead getGroupName

`consolidate-group-reads` - **effort** S | **risk** low | **impact** low

### Problem
The groups router re-inlines group-read shapes that the db/groups.ts helper layer should own. `groups.mine` hand-builds a name map (router lines 62-65, 71) that `getGroupNames` already returns identically, and member count is coerced two different ways across `mine` (lines 66-72) and `previewByCode` (lines 127-131), each with its own `count()` + `Number()`. Meanwhile `getGroupName` (singular, groups.ts lines 47-50) has zero callers. This epic does the two high-value, behavior-preserving edits - route `mine`'s name map through `getGroupNames`, and delete the dead `getGroupName` (rewording the `getGroupNames` doc comment that references it) - plus an OPTIONAL count-coercion helper. It explicitly SKIPS the low-value `getGroup(id)` extraction and does NOT touch `removeMember` (its member read is a transactional isMember guard, not a count() site).

### Why it matters
getGroupNames already exists, is the canonical bulk read (events.ts:431 uses it), and returns a behavior-identical Map<id,name>; mine re-inlines it with a narrower `.select({ id, name })` projection - the divergence is pure duplication. Deleting getGroupName removes confirmed dead code and the stale doc reference to it. Behavior is preserved: same inArray query, same key/value, same caller-side FALLBACK_GROUP_NAME and member-count maps.

### Change steps
- mine: import getGroupNames alongside FALLBACK_GROUP_NAME on apps/api/src/routers/groups.ts:14, then replace the nameRows select (lines 62-65) and the nameMap construction (line 71) with `const nameMap = await getGroupNames(groupIds);` - leaving the countRows query, countMap, and the `?? FALLBACK_GROUP_NAME` at line 76 untouched.
- Dead code: delete getGroupName (groups.ts lines 47-50) - it has zero callers (verified: only the definition and a doc-comment reference exist).
- Reword the getGroupNames doc comment (groups.ts line 52) so it no longer says 'Bulk version of getGroupName' - e.g. 'Resolve many group names in ONE inArray query, returning a Map<groupId, name>.'
- OPTIONAL (low value): add a bulk member-count helper to db/groups.ts (e.g. `memberCounts(ids: string[]): Promise<Map<string, number>>`, empty-list short-circuit like getGroupNames) that owns the count()+Number() coercion once; back mine's countMap with it and have previewByCode call `(await memberCounts([group.id])).get(group.id) ?? 0`. Do NOT touch removeMember and do NOT claim it as a consumer.
- SKIP: getGroup(id) single-row extraction (self-admitted low value, two divergent null-vs-throw call sites).

### Before / after
```ts
// db/groups.ts - BEFORE
export async function getGroupName(id: string): Promise<string> {
  const [g] = await db.select().from(groups).where(eq(groups.id, id));
  return g?.name ?? FALLBACK_GROUP_NAME;
}
// Bulk version of getGroupName: resolve many group names in ONE inArray query, returning a
// Map<groupId, name>. ...
export async function getGroupNames(ids: string[]): Promise<Map<string, string>> { ... }

// db/groups.ts - AFTER (getGroupName deleted, comment reworded)
// Resolve many group names in ONE inArray query, returning a Map<groupId, name>. Callers read
// names via the map (with FALLBACK_GROUP_NAME for missing rows), avoiding a per-row SELECT. An
// empty id list issues no query and returns an empty map.
export async function getGroupNames(ids: string[]): Promise<Map<string, string>> { ... }

// routers/groups.ts:14 - BEFORE
import { FALLBACK_GROUP_NAME, freshInviteCode, inviteUrlFor } from "../db/groups.js";
// AFTER
import { FALLBACK_GROUP_NAME, freshInviteCode, getGroupNames, inviteUrlFor } from "../db/groups.js";

// routers/groups.ts mine - BEFORE (lines 62-65, 71)
const nameRows = await db
  .select({ id: groups.id, name: groups.name })
  .from(groups)
  .where(inArray(groups.id, groupIds));
const countRows = await db.select(...).from(groupMembers)...; // untouched
const nameMap = new Map(nameRows.map((r) => [r.id, r.name]));
const countMap = new Map(countRows.map((r) => [r.groupId, Number(r.n)]));
// AFTER
const nameMap = await getGroupNames(groupIds);
const countRows = await db.select(...).from(groupMembers)...; // untouched
const countMap = new Map(countRows.map((r) => [r.groupId, Number(r.n)]));
// .map(...) result rows unchanged: name uses `nameMap.get(m.groupId) ?? FALLBACK_GROUP_NAME` (line 76)

// NOTE: `inArray` may become unused in routers/groups.ts after this swap - removeMember still uses
// inArray (lines 206/211/215), so the import stays; verify with `pnpm lint`/typecheck.
```

### Files
- `apps/api/src/routers/groups.ts`
- `apps/api/src/db/groups.ts`

---

## 9. Share memberIdsOf so events.ts stops re-inlining the member-id query

`share-memberidsof-db-helper` - **effort** S | **risk** low | **impact** low

### Problem
groups.ts defines a module-private memberIdsOf(groupId) (lines 33-39) that runs `select({ userId }).from(groupMembers).where(eq(groupMembers.groupId, groupId))` and maps to user ids. events.get re-inlines the exact same query (events.ts:980-983) and does the same `.map((m) => m.userId)` (line 986) before handing the ids to loadEventBundle. Promote the query to one shared db helper in db/groups.ts (the established home for group-scoped reads, alongside getGroupName/getGroupNames) and have both routers reuse it. Behavior-preserving dedup; one definition of the membership-id query.

### Why it matters
groups.ts already imports requireMember FROM events.ts, and db/groups.ts is the established home for group-scoped reads (getGroupName/getGroupNames), so db/groups.ts - not events.ts - is the correct target. This collapses two identical hand-written membership-id queries into one definition, so a future schema/index change to how member ids are read happens in one place instead of drifting between the two routers.

### Change steps
- Add `groupMembers` to the existing `./schema.js` import in apps/api/src/db/groups.ts (currently only imports `groups`).
- Move the `memberIdsOf(groupId)` definition into apps/api/src/db/groups.ts as an exported function, placed alongside getGroupName/getGroupNames.
- In apps/api/src/routers/groups.ts: delete the private `memberIdsOf` definition (lines 33-39) and add `memberIdsOf` to the existing `from "../db/groups.js"` import (currently `FALLBACK_GROUP_NAME, freshInviteCode, inviteUrlFor`). Call sites at lines 86 and 97 stay unchanged.
- In apps/api/src/routers/events.ts: add `memberIdsOf` to the existing `from "../db/groups.js"` import (currently `FALLBACK_GROUP_NAME, getGroupNames`). Replace the inline select at lines 980-983 and the `.map((m) => m.userId)` at line 986 with `const memberIds = await memberIdsOf(e.groupId);` and pass `memberIds` as the second arg to loadEventBundle.
- Run pnpm lint, pnpm typecheck, pnpm test --filter @bethere/api to confirm no behavior change.

### Before / after
```ts
// apps/api/src/db/groups.ts
//   import: add groupMembers
- import { groups } from "./schema.js";
+ import { groupMembers, groups } from "./schema.js";

+ // The user ids of a group's members, in row order. The single source for this membership-id
+ // query, shared by groupsRouter (get / addableUsers) and events.get.
+ export async function memberIdsOf(groupId: string): Promise<string[]> {
+   const rows = await db
+     .select({ userId: groupMembers.userId })
+     .from(groupMembers)
+     .where(eq(groupMembers.groupId, groupId));
+   return rows.map((r) => r.userId);
+ }

// apps/api/src/routers/groups.ts
- import { FALLBACK_GROUP_NAME, freshInviteCode, inviteUrlFor } from "../db/groups.js";
+ import { FALLBACK_GROUP_NAME, freshInviteCode, inviteUrlFor, memberIdsOf } from "../db/groups.js";
- async function memberIdsOf(groupId: string): Promise<string[]> {
-   const rows = await db
-     .select({ userId: groupMembers.userId })
-     .from(groupMembers)
-     .where(eq(groupMembers.groupId, groupId));
-   return rows.map((r) => r.userId);
- }
  // call sites at lines 86 (get) and 97 (addableUsers) unchanged

// apps/api/src/routers/events.ts  (events.get)
- import { FALLBACK_GROUP_NAME, getGroupNames } from "../db/groups.js";
+ import { FALLBACK_GROUP_NAME, getGroupNames, memberIdsOf } from "../db/groups.js";
- const memberRows = await db
-   .select({ userId: groupMembers.userId })
-   .from(groupMembers)
-   .where(eq(groupMembers.groupId, e.groupId));
- const bundle = await loadEventBundle(
-   [e],
-   memberRows.map((m) => m.userId),
- );
+ const memberIds = await memberIdsOf(e.groupId);
+ const bundle = await loadEventBundle([e], memberIds);
```

### Files
- `apps/api/src/db/groups.ts`
- `apps/api/src/routers/groups.ts`
- `apps/api/src/routers/events.ts`

---

## 10. Drive pgEnum value tuples from the shared z.enum single source of truth

`drive-pgenum-from-shared-zenum` - **effort** S | **risk** low | **impact** med

### Problem
Four enum value tuples (response_kind, plan_phase, part_of_day, candidate_kind) are hardcoded verbatim in BOTH apps/api/src/db/schema.ts (as pgEnum literals) and packages/shared/src/schemas.ts (as z.enum), even though the shared file's comments call themselves "the single source of truth for the DB ... enum." This is silent-drift risk: changing one z.enum does not update the pgEnum, and nothing fails. The api db layer already runtime value-imports @bethere/shared (db/groups.ts, db/seed-data.ts), and zod's `.options` is a readonly non-empty literal tuple - exactly the `Readonly<[U, ...U[]]>` shape drizzle's pgEnum accepts. Feed the pgEnum calls from the shared enums' `.options` so schemas.ts becomes the literal single source it already claims to be. Behavior-preserving: value sets and order are identical, so the generated SQL enum is unchanged, and migrations are hand-authored (no regen). Leave eventStatusEnum ["open","resolved"] (schema.ts:15) hardcoded - it is frozen legacy with no z.enum mirror.

### Why it matters
The shared schemas.ts comments explicitly claim to be "the single source of truth for the DB candidate_kind enum and the mobile mirror," but the DB pgEnum tuples are hand-copied duplicates that silently drift: edit a z.enum and the pgEnum stays stale with zero compile error. This wires the DB definition to the declared source so the two can never diverge, at near-zero cost and risk (one import line plus four trivial substitutions, behavior identical).

### Change steps
- Add a runtime value-import at the top of apps/api/src/db/schema.ts: `import { CandidateKind, PartOfDay, PlanPhase, ResponseKind } from "@bethere/shared";` (mirrors the existing runtime imports in db/groups.ts and db/seed-data.ts).
- Replace schema.ts:16 `pgEnum("response_kind", ["yes", "no", "conditional"])` with `pgEnum("response_kind", ResponseKind.options)`.
- Replace schema.ts:19 `pgEnum("plan_phase", ["collecting", "moment", "cleared", "fizzled"])` with `pgEnum("plan_phase", PlanPhase.options)`.
- Replace schema.ts:21 `pgEnum("part_of_day", ["morning", "afternoon", "evening", "late"])` with `pgEnum("part_of_day", PartOfDay.options)`.
- Replace schema.ts:23 `pgEnum("candidate_kind", ["time", "activity"])` with `pgEnum("candidate_kind", CandidateKind.options)`.
- Leave eventStatusEnum (schema.ts:15) as the hardcoded ["open","resolved"] array - frozen legacy with no shared z.enum counterpart.
- Verify: `pnpm --filter @bethere/api typecheck` and `pnpm test` (and `pnpm lint`). No migration SQL changes - migrations are hand-authored and the value sets/order are byte-for-byte identical.

### Before / after
```ts
// apps/api/src/db/schema.ts

// BEFORE (top imports unchanged, then):
export const eventStatusEnum = pgEnum("event_status", ["open", "resolved"]);
export const responseKindEnum = pgEnum("response_kind", ["yes", "no", "conditional"]);
export const planPhaseEnum = pgEnum("plan_phase", ["collecting", "moment", "cleared", "fizzled"]);
export const partOfDayEnum = pgEnum("part_of_day", ["morning", "afternoon", "evening", "late"]);
export const candidateKindEnum = pgEnum("candidate_kind", ["time", "activity"]);

// AFTER (add the runtime value-import alongside the drizzle import):
import { CandidateKind, PartOfDay, PlanPhase, ResponseKind } from "@bethere/shared";

// eventStatusEnum stays hardcoded - frozen legacy, no z.enum mirror:
export const eventStatusEnum = pgEnum("event_status", ["open", "resolved"]);
export const responseKindEnum = pgEnum("response_kind", ResponseKind.options);
export const planPhaseEnum = pgEnum("plan_phase", PlanPhase.options);
export const partOfDayEnum = pgEnum("part_of_day", PartOfDay.options);
export const candidateKindEnum = pgEnum("candidate_kind", CandidateKind.options);

// Why this is safe / behavior-preserving:
// - drizzle pgEnum<U, T extends Readonly<[U, ...U[]]>> accepts a readonly non-empty
//   literal tuple; z.enum(...).options IS that exact shape (verified in
//   drizzle-orm/pg-core/columns/enum.d.ts:45).
// - The shared enum value sets AND order match the old hardcoded tuples 1:1, so the
//   generated SQL enum is unchanged.
// - Migrations are hand-authored (db:generate is unusable/interactive here), so there
//   is no regen step and no migration diff.
```

### Files
- `apps/api/src/db/schema.ts`

---

## 11. Parameterize the repeated FieldEdit.refine(max, message) in UpdateEventInput

`parameterize-fieldedit-max-refine` - **effort** S | **risk** low | **impact** low

### Problem
UpdateEventInput in packages/shared/src/schemas.ts spells the same "cap the CAS `to` value at a max length" rule three times (activity/location/description), differing only in the cap constant and message. Extract a file-local unexported factory `boundedFieldEdit(max, message)` just above UpdateEventInput and rewrite the three fields through it. Purely behavior-preserving: `.refine` returns the same ZodEffects, the inferred field type stays `{ from: string; to: string }`, and the router (apps/api/src/routers/events.ts:853-865) only reads `.from`/`.to`. CreateEventInput is untouched (it uses inline z.string().max, not FieldEdit). Existing schemas.test.ts coverage (lines 220-313) continues to assert the same outcomes.

### Why it matters
The "cap the CAS `to` value at a max length" rule is one idea written three times. Collapsing it to a single factory means adding a fourth editable field, or changing the predicate (e.g. trimming before measuring length), is one edit instead of three near-identical refine bodies to keep in sync.

### Change steps
- In packages/shared/src/schemas.ts, after the FieldEdit definition (line 123) and before UpdateEventInput (line 129), add a file-local unexported helper: const boundedFieldEdit = (max: number, message: string) => FieldEdit.refine((f) => f.to.length <= max, { message });
- Rewrite UpdateEventInput.activity (currently lines 130-132) as: activity: boundedFieldEdit(ACTIVITY_MAX, "activity is too long").optional(),
- Rewrite UpdateEventInput.location (currently lines 133-135) as: location: boundedFieldEdit(LOCATION_MAX, "location is too long").optional(),
- Rewrite UpdateEventInput.description (currently lines 136-138) as: description: boundedFieldEdit(DESCRIPTION_MAX, "notes are too long").optional(),
- Leave CreateEventInput (lines 69-72) and the ACTIVITY_MAX/LOCATION_MAX/DESCRIPTION_MAX constants (lines 47-49) unchanged; keep all caps and messages identical.
- Run pnpm typecheck and the schemas.test.ts suite (pnpm --filter @bethere/shared test) to confirm green.

### Before / after
```ts
Before (packages/shared/src/schemas.ts:129-139):

  export const UpdateEventInput = ByEvent.extend({
    activity: FieldEdit.refine((f) => f.to.length <= ACTIVITY_MAX, {
      message: "activity is too long",
    }).optional(),
    location: FieldEdit.refine((f) => f.to.length <= LOCATION_MAX, {
      message: "location is too long",
    }).optional(),
    description: FieldEdit.refine((f) => f.to.length <= DESCRIPTION_MAX, {
      message: "notes are too long",
    }).optional(),
  });

After:

  // A FieldEdit whose `to` value is length-capped, with a field-specific message.
  const boundedFieldEdit = (max: number, message: string) =>
    FieldEdit.refine((f) => f.to.length <= max, { message });

  export const UpdateEventInput = ByEvent.extend({
    activity: boundedFieldEdit(ACTIVITY_MAX, "activity is too long").optional(),
    location: boundedFieldEdit(LOCATION_MAX, "location is too long").optional(),
    description: boundedFieldEdit(DESCRIPTION_MAX, "notes are too long").optional(),
  });
```

### Files
- `packages/shared/src/schemas.ts`

---

## 12. Reuse the shared Conditional type and ActivityText rule instead of re-spelling them

`reuse-conditional-and-activitytext` - **effort** S | **risk** low | **impact** low

### Problem
Two single-source shared shapes are bypassed by inline copies. The Conditional shape ({mode:'all'|'any'; targetIds:string[]}) is hand-spelled in apps/api/src/db/schema.ts:158 (the jsonb $type) and in packages/shared/src/logic/resolve.ts:6 (MomentResponse.cond), even though packages/shared/src/schemas.ts already exports a canonical `Conditional` type that schemas.ts:145 and apps/api/src/db/seed-data.ts:62 consume. (Note: the proposal's premise that Conditional has no consumers is wrong - seed-data.ts:62 already uses it, so this unifies 3 inline sites, not zero.) Separately, CreateEventInput.activityCandidates (schemas.ts:72) re-inlines the activity-text rule as `z.string().trim().min(1).max(ACTIVITY_MAX)` instead of reusing the exported `ActivityText` constant that AddCandidateInput.text (schemas.ts:99) uses. Wire both inline copies back to the canonical definitions. Behavior is preserved: the inferred TS shape for Conditional is identical at all sites, and for the activity rule the trim is simply relocated into ActivityText itself (verified that both the create path at events.ts:502 and the addCandidate path at events.ts:770 already re-trim server-side, and that schemas.test.ts:101-110 requires CreateEventInput to keep trimming).

### Why it matters
These are single-source-of-truth shapes whose canonical definitions already exist and are exported but partially bypassed by hand-spelled copies. The inline copies are correctness drift waiting to happen: if Conditional ever gains a field or the activity caps change, the inline jsonb $type, resolve.ts interface, and the create array would silently diverge from the schema. Wiring them back makes the shapes follow their source automatically (the documented type-chain convention), with zero behavior change.

### Change steps
- Conditional / schema.ts: in apps/api/src/db/schema.ts add `import type { Conditional } from "@bethere/shared"`, then replace the responses.cond jsonb generic at line 158 with `.$type<Conditional>()`.
- Conditional / resolve.ts: in packages/shared/src/logic/resolve.ts add `Conditional` to the existing `import type { ResponseKind } from "../schemas.js"` (making it `import type { Conditional, ResponseKind } from "../schemas.js"`), then change MomentResponse.cond at line 6 from the inline object literal to `cond?: Conditional`.
- Conditional / comment: re-check the comment at schemas.ts:10 ("`conditional` carries a `cond` (see Conditional)") - it is still accurate, so leave it unless wording drifts.
- ActivityText / make it carry the trim: in packages/shared/src/schemas.ts:53 change `export const ActivityText = z.string().min(1).max(ACTIVITY_MAX)` to `export const ActivityText = z.string().trim().min(1).max(ACTIVITY_MAX)`.
- ActivityText / reuse in CreateEventInput: in schemas.ts:72 change `activityCandidates: z.array(z.string().trim().min(1).max(ACTIVITY_MAX)).max(10).optional()` to `activityCandidates: z.array(ActivityText).max(10).optional()`. Do NOT use an un-trimmed ActivityText composed into the array (that drops the create-side trim and breaks schemas.test.ts:101-110).
- ActivityText / comment: update the now-inaccurate comment at schemas.ts:51-52 - it currently says "The create array additionally trims; see CreateEventInput." After this change the trim lives in ActivityText itself, so reword to note that ActivityText trims for both create and addCandidate.
- Verify: run `pnpm typecheck` and `pnpm test`, especially packages/shared/src/schemas.test.ts, apps/api/src/routers/events-addCandidate.test.ts, and apps/api/src/routers/events-create.test.ts, and confirm green.

### Before / after
```ts
// --- Conditional rewire ---
// packages/shared/src/logic/resolve.ts
// BEFORE
import type { ResponseKind } from "../schemas.js";
export interface MomentResponse {
  userId: string;
  kind: ResponseKind;
  cond?: { mode: "all" | "any"; targetIds: string[] };   // line 6 inline copy
}
// AFTER
import type { Conditional, ResponseKind } from "../schemas.js";
export interface MomentResponse {
  userId: string;
  kind: ResponseKind;
  cond?: Conditional;
}

// apps/api/src/db/schema.ts
// BEFORE
import { boolean, integer, jsonb, /* ... */ } from "drizzle-orm/pg-core";
// ...
cond: jsonb("cond").$type<{ mode: "all" | "any"; targetIds: string[] }>(),  // line 158
// AFTER
import type { Conditional } from "@bethere/shared";
// ...
cond: jsonb("cond").$type<Conditional>(),

// --- ActivityText reuse ---
// packages/shared/src/schemas.ts
// BEFORE
export const ActivityText = z.string().min(1).max(ACTIVITY_MAX);              // line 53
// ...
activityCandidates: z.array(z.string().trim().min(1).max(ACTIVITY_MAX)).max(10).optional(), // line 72
// AFTER
export const ActivityText = z.string().trim().min(1).max(ACTIVITY_MAX);
// ...
activityCandidates: z.array(ActivityText).max(10).optional(),
```

### Files
- `packages/shared/src/schemas.ts`
- `packages/shared/src/logic/resolve.ts`
- `apps/api/src/db/schema.ts`

---

## 13. Consume activeDeadline's label in EventDetail's CountdownBanner instead of re-hardcoding the phase->label mapping

`drp-eventdetail-countdown-label-dedup` - **effort** S | **risk** low | **impact** low

### Problem
In EventDetail.tsx the two mutually-exclusive CountdownBanner branches (lines 436 and 439) hand-pick DEADLINE_VOTING vs DEADLINE_RSVP by phase, duplicating the exact phase->label mapping that activeDeadline (lib/status.ts:71-75) already returns and that the dashboard action cards already consume. The screen calls deadlineMs(data, now) on line 330, which internally calls activeDeadline(data) and throws away its label. This refactor reads `const { label } = activeDeadline(data);` once next to the existing `ms` and passes that shared label into both banner branches, keeping `note` (NOTE_TOP_PICK / NOTE_BLIND) inline since activeDeadline does not carry it. The now-unused DEADLINE_VOTING/DEADLINE_RSVP imports are dropped from the copy import block. Behavior-preserving: each banner branch is already gated by the same phase condition that drives activeDeadline's mapping (collecting->DEADLINE_VOTING, moment->DEADLINE_RSVP), so the rendered text is identical per phase; the banner can no longer drift from the dashboard. Part 1 (TimeHero extraction) is intentionally dropped per verifier: single call site (no dedup), does not cleanly isolate the raw-Text concern because the same raw typography recurs at lines 484-492, and the hero's fontSize 34 has no AppText variant to map onto.

### Why it matters
Behavior-preserving dedup. The phase->deadline-label mapping currently lives in two places: activeDeadline (lib/status.ts, the source of truth consumed by the dashboard action cards) and these two inline CountdownBanner props. Because EventDetail already calls deadlineMs - which internally calls activeDeadline(data) and discards its label - the banner can silently drift from the dashboard if the wording or phase-mapping ever changes. Routing the banner through the same helper makes the mapping single-sourced and the two surfaces impossible to desync, with zero user-visible change.

### Change steps
- In apps/mobile/src/lib/status.ts: no change needed - activeDeadline is already exported (line 71) and returns { iso, label } with the phase->label mapping (collecting -> DEADLINE_VOTING, moment -> DEADLINE_RSVP).
- In apps/mobile/src/screens/EventDetail.tsx line 21: add activeDeadline to the existing import from ../lib/status (currently `import { deadlineMs, isLive, isTerminal, type Phase } from "../lib/status";`).
- In apps/mobile/src/screens/EventDetail.tsx near line 330: alongside `const ms = deadlineMs(data, now);` add `const { label } = activeDeadline(data);`.
- In apps/mobile/src/screens/EventDetail.tsx line 436: replace `label={DEADLINE_VOTING}` with `label={label}` on the collecting CountdownBanner; keep `ms={ms}` and `note={NOTE_TOP_PICK}` inline.
- In apps/mobile/src/screens/EventDetail.tsx line 439: replace `label={DEADLINE_RSVP}` with `label={label}` on the moment CountdownBanner; keep `ms={ms}` and `note={NOTE_BLIND}` inline.
- In apps/mobile/src/screens/EventDetail.tsx lines 8-9: remove DEADLINE_RSVP and DEADLINE_VOTING from the `../lib/copy` import block (they become unused; NO_NAMES, NOTE_BLIND, NOTE_TOP_PICK, planLabel, statusLabel, etc. stay).
- Run `pnpm lint`, `pnpm typecheck`, and `pnpm test`; confirm no unused-import lint errors and a clean typecheck.

### Before / after
```ts
// apps/mobile/src/screens/EventDetail.tsx

// --- imports (lines 7-18): drop DEADLINE_RSVP, DEADLINE_VOTING ---
// BEFORE
import {
  DEADLINE_RSVP,
  DEADLINE_VOTING,
  DIDNT_COME_TOGETHER,
  ERR_SAVE,
  ...
} from "../lib/copy";
// AFTER
import {
  DIDNT_COME_TOGETHER,
  ERR_SAVE,
  ...
} from "../lib/copy";

// --- import line 21: add activeDeadline ---
// BEFORE
import { deadlineMs, isLive, isTerminal, type Phase } from "../lib/status";
// AFTER
import { activeDeadline, deadlineMs, isLive, isTerminal, type Phase } from "../lib/status";

// --- near line 330 ---
// BEFORE
const ms = deadlineMs(data, now);
// AFTER
const ms = deadlineMs(data, now);
const { label } = activeDeadline(data); // collecting -> DEADLINE_VOTING, moment -> DEADLINE_RSVP

// --- banner branches (lines 435-440): note stays inline ---
// BEFORE
{data.phase === "collecting" && data.decidesBy && (
  <CountdownBanner label={DEADLINE_VOTING} ms={ms} note={NOTE_TOP_PICK} />
)}
{data.phase === "moment" && (
  <CountdownBanner label={DEADLINE_RSVP} ms={ms} note={NOTE_BLIND} />
)}
// AFTER
{data.phase === "collecting" && data.decidesBy && (
  <CountdownBanner label={label} ms={ms} note={NOTE_TOP_PICK} />
)}
{data.phase === "moment" && (
  <CountdownBanner label={label} ms={ms} note={NOTE_BLIND} />
)}
```

### Files
- `apps/mobile/src/screens/EventDetail.tsx`

---

## 14. Rename MomentView's editing/onEdit which collide with the edit-details vocabulary

`rename-momentview-editing-onedit` - **effort** S | **risk** low | **impact** low

### Problem
EventDetail.tsx overloads the word "edit" for two unrelated flows. The `editing`/`setEditing` state and the MomentView `editing`/`onEdit` props (EventDetail.tsx lines 71, 207, 216, 531-532, 795-796, 804-805, 811, 848) do NOT edit any plan content: `editing` toggles whether the blind-moment RSVP answer choices are shown, and `onEdit` is wired to `changeAnswer` (which calls `events.unrespond`) behind a button literally labeled "Change". Meanwhile the genuine edit-details sheet uses a separate, correctly-named vocabulary (`editSheet`/`openEditSheet`/`canEditDetails`, plus `editActivity`/`editLocation`/`editNotes`) for the events.update compare-and-set flow. A reader scanning the file conflates the RSVP-re-answer toggle with editing details. Rename the RSVP-change concept so it no longer borrows "edit": `editing`/`setEditing` -> `reanswering`/`setReanswering`, MomentView prop `editing` -> `reanswering`, and `onEdit` -> `onChangeAnswer` (matching the existing `changeAnswer` handler and the "Change" button). This is a pure mechanical rename, behavior-preserving, confined to EventDetail.tsx, plus a one-line stale-comment fix in useBusyAction.ts:18 which currently references the soon-to-be-renamed `setEditing(false)` symbol. CreateWizard.tsx's DeadlineField has its own independent `editing`/`onEdit` props (lines 742, 753) and must NOT be touched. Verified: the only `editing`/`setEditing`/`onEdit` references in EventDetail.tsx are the 11 cited lines; the EventDetail/Groups test files only use the English word "editing" in prose and need no change.

### Why it matters
The word "edit" carries two unrelated meanings in EventDetail.tsx, the most logic-dense screen in the app. The MomentView `editing`/`onEdit` names suggest they participate in the edit-details (events.update / compare-and-set) flow, but they actually drive the blind-moment RSVP re-answer toggle (events.unrespond, "Change" button). A maintainer reasoning about edit-details permissions (canEditDetails, editSheet) can easily mis-trace this. Renaming to `reanswering`/`onChangeAnswer` aligns the names with the existing `changeAnswer` handler and the "Change" label, cleanly separating the two flows with zero behavior change.

### Change steps
- EventDetail.tsx:71 - rename state declaration `const [editing, setEditing] = useState(false)` -> `const [reanswering, setReanswering] = useState(false)`
- EventDetail.tsx:207 - in answer(), `setEditing(false)` -> `setReanswering(false)`
- EventDetail.tsx:216 - in changeAnswer(), `setEditing(true)` -> `setReanswering(true)`
- EventDetail.tsx:531-532 - MomentView call site: `editing={editing}` -> `reanswering={reanswering}` and `onEdit={changeAnswer}` -> `onChangeAnswer={changeAnswer}`
- EventDetail.tsx:795-796 - MomentView destructured params: `editing` -> `reanswering`, `onEdit` -> `onChangeAnswer`
- EventDetail.tsx:804-805 - MomentView prop type: `editing: boolean` -> `reanswering: boolean`, `onEdit: () => void` -> `onChangeAnswer: () => void`
- EventDetail.tsx:811 - `const showAnswer = editing || !data.myResponse` -> `const showAnswer = reanswering || !data.myResponse`
- EventDetail.tsx:848 - 'Change' button `onPress={onEdit}` -> `onPress={onChangeAnswer}`
- useBusyAction.ts:18 - update the stale comment that references `setEditing(false)` to `setReanswering(false)` so it points at the renamed symbol
- Run `pnpm typecheck` to confirm no dangling references; do NOT touch CreateWizard.tsx's DeadlineField (its independent editing/onEdit props)

### Before / after
```ts
// EventDetail.tsx

// BEFORE
const [editing, setEditing] = useState(false);              // L71

function answer(...) {
  return runAction(async () => {
    await trpc.events.respond.mutate(...);
    setEditing(false);                                       // L207
  });
}
function changeAnswer() {
  return runAction(async () => {
    await trpc.events.unrespond.mutate({ eventId });
    setEditing(true);                                        // L216
  });
}

<MomentView
  ...
  editing={editing}                                          // L531
  onEdit={changeAnswer}                                      // L532
/>

function MomentView({ data, busy, editing, onEdit, ... }: {  // L795-796
  ...
  editing: boolean;                                          // L804
  onEdit: () => void;                                        // L805
}) {
  const showAnswer = editing || !data.myResponse;            // L811
  ...
  <Button label="Change" ... onPress={onEdit} ... />         // L848
}

// AFTER
const [reanswering, setReanswering] = useState(false);

    setReanswering(false);   // in answer()
    setReanswering(true);    // in changeAnswer()

<MomentView
  ...
  reanswering={reanswering}
  onChangeAnswer={changeAnswer}
/>

function MomentView({ data, busy, reanswering, onChangeAnswer, ... }: {
  ...
  reanswering: boolean;
  onChangeAnswer: () => void;
}) {
  const showAnswer = reanswering || !data.myResponse;
  ...
  <Button label="Change" ... onPress={onChangeAnswer} ... />
}

// useBusyAction.ts:18 comment
// BEFORE: ... e.g. EventDetail's `setEditing(false)` before reload ...
// AFTER:  ... e.g. EventDetail's `setReanswering(false)` before reload ...
```

### Files
- `apps/mobile/src/screens/EventDetail.tsx`
- `apps/mobile/src/lib/useBusyAction.ts`

---

## 15. De-duplicate the What/When confirm-step SummaryItem note logic in CreateWizard

`dedup-createwizard-axis-summary-note` - **effort** S | **risk** low | **impact** low

### Problem
The confirm step in CreateWizard renders two structurally identical SummaryItems ("What" and "When") whose `note` props are the same three-branch expression (no candidates -> undefined; locked -> "Locked - the group can't add more"; else -> "Open - the group can add more and vote"), differing only in which axis arrays/flags feed it. Each branch and both literal strings are written out twice (lines 529-535 and 541-547 of apps/mobile/src/screens/CreateWizard.tsx). Extract one tiny pure helper `axisNote(hasCandidates: boolean, locked: boolean): string | undefined` (placed alongside the file's existing module-level helpers like makeDeadlineHandlers) and call it at both sites. Behavior-preserving: identical output for every input.

### Why it matters
The two confirm-step SummaryItem note expressions are an exact three-branch duplicate differing only in axis inputs, and the two literal note strings ("Locked - the group can't add more" and "Open - the group can add more and vote") are each written twice. Collapsing them into one pure helper removes the copy-paste, guarantees the two axes stay in sync, and keeps the single source for the wording. Confirmed via grep that both literals appear ONLY at these two sites - nowhere else in mobile and not in copy.ts - so a local helper (not a copy.ts promotion) is the right scope.

### Change steps
- Add a module-level pure helper `axisNote(hasCandidates: boolean, locked: boolean): string | undefined` to apps/mobile/src/screens/CreateWizard.tsx, near the existing makeDeadlineHandlers helper. It returns undefined when !hasCandidates, the "Locked - the group can't add more" string when locked, and the "Open - the group can add more and vote" string otherwise. The two literals stay inline inside the helper.
- Replace the "What" SummaryItem note prop (currently the inline ternary on lines 529-535) with note={axisNote(summaryActivities.length > 0, lockActivityEff)}.
- Replace the "When" SummaryItem note prop (currently the inline ternary on lines 541-547) with note={axisNote(summaryTimes.length > 0, lockTimesEff)}.
- Leave the `lines` and `muted` props on both SummaryItems unchanged (their empty-fallback strings differ per axis: "Open - the group adds ideas" vs "Open - the group adds times"), so they stay inline.
- Run pnpm lint, pnpm typecheck, pnpm test to confirm the cleanup is behavior-preserving.

### Before / after
```ts
BEFORE (apps/mobile/src/screens/CreateWizard.tsx, lines 525-548):
  <SummaryItem
    label="What"
    lines={summaryActivities.length ? summaryActivities : ["Open - the group adds ideas"]}
    muted={summaryActivities.length === 0}
    note={
      summaryActivities.length === 0
        ? undefined
        : lockActivityEff
          ? "Locked - the group can't add more"
          : "Open - the group can add more and vote"
    }
  />
  <SummaryItem
    label="When"
    lines={summaryTimes.length ? summaryTimes : ["Open - the group adds times"]}
    muted={summaryTimes.length === 0}
    note={
      summaryTimes.length === 0
        ? undefined
        : lockTimesEff
          ? "Locked - the group can't add more"
          : "Open - the group can add more and vote"
    }
  />

AFTER (call sites):
  <SummaryItem
    label="What"
    lines={summaryActivities.length ? summaryActivities : ["Open - the group adds ideas"]}
    muted={summaryActivities.length === 0}
    note={axisNote(summaryActivities.length > 0, lockActivityEff)}
  />
  <SummaryItem
    label="When"
    lines={summaryTimes.length ? summaryTimes : ["Open - the group adds times"]}
    muted={summaryTimes.length === 0}
    note={axisNote(summaryTimes.length > 0, lockTimesEff)}
  />

AFTER (new module-level helper, beside makeDeadlineHandlers near line 594):
  // Confirm-step note shown under a What/When SummaryItem: nothing when the axis
  // has no candidates, otherwise whether the group can still add to that list.
  function axisNote(hasCandidates: boolean, locked: boolean): string | undefined {
    if (!hasCandidates) return undefined;
    return locked
      ? "Locked - the group can't add more"
      : "Open - the group can add more and vote";
  }
```

### Files
- `apps/mobile/src/screens/CreateWizard.tsx`

---

## 16. Express isConcrete from timeFixed/activityFixed and factor the reply-close deadline line

`create-wizard-concrete-and-reply-line-dedup` - **effort** S | **risk** low | **impact** low

### Problem
In apps/mobile/src/screens/CreateWizard.tsx, the concrete-shortcut invariant is spelled out three times. isConcrete (line 170) is the AND of two predicates - one time axis pinned (timeIsos.length === 1 && lockTimesEff) and the activity axis pinned (lockActivityEff && activityCount <= 1) - but those same conjuncts are re-derived inline in the outcomeSummary() call (lines 566-567) as timeFixed/activityFixed, and the function's comment at 830-831 then has to re-explain that wiring in prose. The relationship isConcrete === timeFixed && activityFixed is real and verified but invisible. Part 1: surface timeFixed/activityFixed as named locals next to isConcrete and define isConcrete = timeFixed && activityFixed; pass the locals into the outcomeSummary call and trim the now-redundant re-spelling in the 830-831 comment. Part 2 (adjacent, independent): the reply-close string `Replies close ${formatSlot(replyShown)}` is built identically in two branches of deadlineLines (lines 229 and 234); factor it once as replyLine. Both parts are pure rename/extract with identical computed values - no behavior change. Out of scope (verified, leave untouched): the defaultLine at 512, which uses a different value (autoReplyIso, not replyShown/replyToSend), and the optional lib/copy.ts "Replies close" prefix extraction.

### Why it matters
Removes a triple-spelled invariant. isConcrete is literally timeFixed && activityFixed, but today that is recomputed inline at the outcomeSummary call and re-explained in prose at 830-831, so the three sites can silently drift. Naming the two predicates once makes the concrete-shortcut invariant self-evident and gives the outcomeSummary call the same locals instead of a copy. The reply-close deadline string is built character-for-character twice in adjacent deadlineLines branches; factoring it to one replyLine local kills the duplication. Pure dedup/extract, zero behavior change, and it sharpens the relationship the long comment was straining to describe.

### Change steps
- Part 1, step 1: In the component body, after activityCount/lockTimesEff/lockActivityEff are computed (lines 161/166/167), introduce two named booleans just above the current isConcrete at line 170: const timeFixed = timeIsos.length === 1 && lockTimesEff; and const activityFixed = lockActivityEff && activityCount <= 1;
- Part 1, step 2: Replace the isConcrete definition at line 170 with const isConcrete = timeFixed && activityFixed; (same four conjuncts, AND is associative/commutative so the value is unchanged). Keep the existing 168-169 comment about the concrete shortcut.
- Part 1, step 3: In the outcomeSummary({...}) call at lines 566-567, replace timeFixed: timeIsos.length === 1 && lockTimesEff and activityFixed: lockActivityEff && activityCount <= 1 with the shorthand timeFixed, and activityFixed, (the named locals).
- Part 1, step 4: In the outcomeSummary function comment at lines 830-831, trim the now-redundant prose that re-spells the caller wiring ("Caller wires activityFixed = lockActivityEff && activityCount <= 1, and lockActivityEff implies activityCount > 0..."); keep only the load-bearing implication that activityFixed implies exactly one activity, since the wiring is now self-evident at the single definition site.
- Part 2, step 1: Just before the deadlineLines assignment (before line 228), compute the reply-close line once: const replyLine = replyShown ? `Replies close ${formatSlot(replyShown)}` : null; (replyShown === replyToSend, already declared at line 227).
- Part 2, step 2: Rewrite the concrete branch at line 229 to use replyLine: replyLine ? [replyLine] : ["We'll pick a sensible deadline"] (preserving the existing fallback string verbatim).
- Part 2, step 3: Rewrite the open-branch reply spread at line 234 from ...(replyShown ? [`Replies close ${formatSlot(replyShown)}`] : []) to ...(replyLine ? [replyLine] : []).
- Leave line 512 (defaultLine, uses autoReplyIso) and lib/copy.ts untouched - explicitly out of scope.
- Run pnpm lint, pnpm typecheck, and pnpm test (and a quick confirm-step spot-check) to confirm the confirm-step summary, deadline lines, and outcome sentence are byte-identical.

### Before / after
```ts
// apps/mobile/src/screens/CreateWizard.tsx

// --- Part 1: name the conjuncts, define isConcrete from them ---
// BEFORE (line 168-170):
  // Concrete shortcut: skip voting only when BOTH axes are pinned - one locked time AND a locked
  // activity. Must match the server's planOpensMoment so the preview never diverges.
  const isConcrete = timeIsos.length === 1 && lockTimesEff && lockActivityEff && activityCount <= 1;

// AFTER:
  const timeFixed = timeIsos.length === 1 && lockTimesEff;
  const activityFixed = lockActivityEff && activityCount <= 1;
  // Concrete shortcut: skip voting only when BOTH axes are pinned - one locked time AND a locked
  // activity. Must match the server's planOpensMoment so the preview never diverges.
  const isConcrete = timeFixed && activityFixed;

// BEFORE (outcomeSummary call, lines 564-567):
                  timeCount: timeIsos.length,
                  activityCount,
                  timeFixed: timeIsos.length === 1 && lockTimesEff,
                  activityFixed: lockActivityEff && activityCount <= 1,
// AFTER:
                  timeCount: timeIsos.length,
                  activityCount,
                  timeFixed,
                  activityFixed,

// BEFORE (function comment, lines 830-831):
  // Caller wires activityFixed = lockActivityEff && activityCount <= 1, and
  // lockActivityEff implies activityCount > 0, so activityFixed implies exactly one activity.
// AFTER:
  // activityFixed implies exactly one activity (lockActivityEff implies activityCount > 0).

// --- Part 2: factor the reply-close line once (lines 228-235) ---
// BEFORE:
  const deadlineLines = isConcrete
    ? [replyShown ? `Replies close ${formatSlot(replyShown)}` : "We'll pick a sensible deadline"]
    : earliestMs == null
      ? ["Set once there's a time on the table"]
      : [
          ...(decidesShown ? [`Voting closes ${formatSlot(decidesShown)}`] : []),
          ...(replyShown ? [`Replies close ${formatSlot(replyShown)}`] : []),
        ];
// AFTER:
  const replyLine = replyShown ? `Replies close ${formatSlot(replyShown)}` : null;
  const deadlineLines = isConcrete
    ? [replyLine ?? "We'll pick a sensible deadline"]
    : earliestMs == null
      ? ["Set once there's a time on the table"]
      : [
          ...(decidesShown ? [`Voting closes ${formatSlot(decidesShown)}`] : []),
          ...(replyLine ? [replyLine] : []),
        ];

// NOTE: line 512 defaultLine stays `Replies close ${formatSlot(autoReplyIso)}` - autoReplyIso != replyShown, out of scope.
```

### Files
- `apps/mobile/src/screens/CreateWizard.tsx`

---

## 17. Use the shared DEADLINE_VOTING constant in CreateWizard's confirm preview

`create-wizard-deadline-voting-constant` - **effort** S | **risk** low | **impact** low

### Problem
copy.ts exports DEADLINE_VOTING ("Voting closes") as the canonical vote-close label and explicitly documents (file header lines 1-4) that these deadline lines "never drift between screens." It is already consumed by EventDetail's CountdownBanner (EventDetail.tsx:436) and by status.ts:73 (activeDeadline). CreateWizard's confirm-step preview re-inlines the literal `Voting closes ${formatSlot(decidesShown)}` at line 233, even though it already imports several other constants from ../lib/copy (lines 5-13). Import DEADLINE_VOTING and build the line from it. The produced text is byte-for-byte identical, so the change is behavior-preserving. The adjacent "Replies close ..." literals (lines 229, 234, 510, 512) are deliberately left alone: the only RSVP-side constant is DEADLINE_RSVP = "RSVP closes", which is worded differently, so pointing them at it would change visible text. A new DEADLINE_REPLIES constant is optional and explicitly out of scope for this pass.

### Why it matters
copy.ts is the single source of truth for recurring user-facing strings and its header pledges these deadline lines stay identical across the create preview and the live banners (EventDetail CountdownBanner, status.ts activeDeadline). The re-inlined "Voting closes" literal in CreateWizard is exactly the kind of drift that contract exists to prevent: a future edit to the canonical label would silently leave the confirm preview behind. Reusing DEADLINE_VOTING - already imported-from in this file - closes that gap with a byte-identical, zero-risk change.

### Change steps
- In apps/mobile/src/screens/CreateWizard.tsx, add DEADLINE_VOTING to the existing named import from ../lib/copy (the block at lines 5-13, alphabetically before ERR_SAVE).
- At apps/mobile/src/screens/CreateWizard.tsx:233, replace the literal `Voting closes ${formatSlot(decidesShown)}` with `${DEADLINE_VOTING} ${formatSlot(decidesShown)}`.
- Leave the four 'Replies close ...' literals at lines 229, 234, 510, 512 untouched - do NOT fold them into DEADLINE_RSVP ("RSVP closes"), which is worded differently and would change visible text.
- Run pnpm lint, pnpm typecheck, and pnpm test to confirm the change is clean and behavior-preserving.

### Before / after
```ts
// apps/mobile/src/screens/CreateWizard.tsx - import block (lines 5-13)
// BEFORE
import {
  ANON_SEND_BODY,
  ANON_SEND_TITLE,
  ERR_SAVE,
  NOTE_TOP_PICK,
  plural,
  STEP_COPY,
  TITLE_NEW_MEETUP,
} from "../lib/copy";
// AFTER
import {
  ANON_SEND_BODY,
  ANON_SEND_TITLE,
  DEADLINE_VOTING,
  ERR_SAVE,
  NOTE_TOP_PICK,
  plural,
  STEP_COPY,
  TITLE_NEW_MEETUP,
} from "../lib/copy";

// apps/mobile/src/screens/CreateWizard.tsx - deadlineLines (line 233)
// BEFORE
          ...(decidesShown ? [`Voting closes ${formatSlot(decidesShown)}`] : []),
// AFTER
          ...(decidesShown ? [`${DEADLINE_VOTING} ${formatSlot(decidesShown)}`] : []),

// Unchanged on purpose (lines 229/234/510/512): "Replies close ..." literals.
// DEADLINE_RSVP is "RSVP closes" (different wording) - do not reuse it here.
```

### Files
- `apps/mobile/src/screens/CreateWizard.tsx`

---

## 18. Route Dashboard no-groups title + CTA through copy.ts

`dashboard-onboarding-copy-dedup` - **effort** S | **risk** low | **impact** low

### Problem
Dashboard.tsx hardcodes the no-groups empty-state title ("No groups yet", line 298) and primary CTA label ("Create a group", line 303) even though both already exist verbatim in lib/copy.ts as ONBOARD_NO_GROUPS_TITLE and ACTION_CREATE_GROUP. Import those two constants and replace the literals. The change is purely a string-centralization dedup with no rendered-text change, so it is behavior-preserving and the existing Dashboard.test.tsx (which asserts on the literal strings the constants evaluate to) keeps passing. Scope is deliberately narrow: leave the meetup-framed body literal at line 300 (no matching constant; the existing ONBOARD_NO_GROUPS_BODY is a different, group-framed sentence), and leave TAB_LABEL, TAB_COLOR, TABS, and the In/Open status-pill ternary in the screen (single call-site, cohesive with the tab color/option mapping that is not copy).

### Why it matters
copy.ts is the single voice file for user-facing strings, and these two exact strings already live there as ONBOARD_NO_GROUPS_TITLE / ACTION_CREATE_GROUP. Dashboard re-inlining them duplicates copy and risks the two sites drifting out of sync (e.g. a wording tweak applied in copy.ts but not the Dashboard card). Centralizing makes the onboarding title/CTA edit-once and keeps Dashboard consistent with GroupsList, which already consumes the constants.

### Change steps
- In apps/mobile/src/screens/Dashboard.tsx, add ONBOARD_NO_GROUPS_TITLE and ACTION_CREATE_GROUP to the existing named import from "../lib/copy" (the import block at lines 8-16).
- Replace the literal at line 298: <AppText variant="title">No groups yet</AppText> becomes <AppText variant="title">{ONBOARD_NO_GROUPS_TITLE}</AppText>.
- Replace the literal at line 303: label="Create a group" becomes label={ACTION_CREATE_GROUP}.
- Leave the body string at line 300 ("You need a group before you can plan a meetup. Create one to start.") unchanged - it has no matching copy.ts constant and is a single call-site.
- Leave TAB_LABEL, TAB_COLOR, TABS, and the In/Open StatusPill ternary (line 139) in the screen - single call-site, cohesive with the tab option/color mapping which is not user-facing copy.
- Run pnpm lint, pnpm typecheck, and pnpm --filter @bethere/mobile test (Dashboard.test.tsx asserts on "No groups yet"/"Create a group", which the constants evaluate to, so it stays green).

### Before / after
```ts
// apps/mobile/src/screens/Dashboard.tsx

// import block (lines 8-16) - add two names:
import {
  actionsRequiredLabel,
  candidateCountLabel,
  DIDNT_COME_TOGETHER,
  ERR_NETWORK,
  goingCountLabel,
  planLabel,
  TITLE_NEW_MEETUP,
+ ONBOARD_NO_GROUPS_TITLE,
+ ACTION_CREATE_GROUP,
} from "../lib/copy";

// no-groups card (lines 297-308):
  <Card>
-   <AppText variant="title">No groups yet</AppText>
+   <AppText variant="title">{ONBOARD_NO_GROUPS_TITLE}</AppText>
    <AppText variant="caption" style={{ marginTop: 6, lineHeight: 18 }}>
      You need a group before you can plan a meetup. Create one to start.   {/* unchanged: no constant */}
    </AppText>
    <Button
-     label="Create a group"
+     label={ACTION_CREATE_GROUP}
      variant="primary"
      ...
    />
  </Card>

// copy.ts (already present, unchanged):
//   export const ONBOARD_NO_GROUPS_TITLE = "No groups yet";       // line 120
//   export const ACTION_CREATE_GROUP   = "Create a group";        // line 123
```

### Files
- `apps/mobile/src/screens/Dashboard.tsx`

---

## 19. Have compareActions reuse activeDeadline's phase-to-field mapping

`compareActions-reuse-activeDeadline` - **effort** S | **risk** low | **impact** low

### Problem
In apps/mobile/src/lib/status.ts, compareActions (lines 121-128) hand-codes the moment->momentEndsAt / collecting->decidesBy deadline-field mapping that activeDeadline (lines 71-75) already owns as the documented single source. The two can silently drift if a phase ever changes which field is its deadline. compareActions only ever receives moment/collecting items (its single consumer, Dashboard.tsx:233, pre-filters with isActionRequired, which admits only those two phases), and activeDeadline returns exactly momentEndsAt for moment and decidesBy for collecting - so swapping the inline lookup for activeDeadline(x).iso is behavior-identical. The aMoment/bMoment booleans stay; only the deadline read changes.

### Why it matters
Removes a duplicated phase-to-deadline-field mapping so activeDeadline stays the single source of truth. Prevents silent drift if a phase's deadline field ever changes (the inline copy would be missed). Pure behavior-preserving dedup.

### Change steps
- In apps/mobile/src/lib/status.ts compareActions, keep the aMoment/bMoment booleans (lines 122-124) unchanged - they still drive the moments-lead ordering on line 124.
- Replace the deadline read on line 125: `const am = msOr(aMoment ? a.momentEndsAt : a.decidesBy, a.startsAt);` becomes `const am = msOr(activeDeadline(a).iso, a.startsAt);`.
- Replace the deadline read on line 126: `const bm = msOr(bMoment ? b.momentEndsAt : b.decidesBy, b.startsAt);` becomes `const bm = msOr(activeDeadline(b).iso, b.startsAt);`.
- No import change is needed - activeDeadline is already exported above in the same file. The aMoment/bMoment booleans remain referenced on line 124, so no unused-variable warning.
- Run pnpm lint, pnpm typecheck, and pnpm test (the Dashboard.test.tsx case at line 253 asserting moments lead the panel is the behavior guard).

### Before / after
```ts
// apps/mobile/src/lib/status.ts - compareActions (lines 121-128)

// BEFORE
export function compareActions(a: Timed, b: Timed): number {
  const aMoment = a.phase === "moment";
  const bMoment = b.phase === "moment";
  if (aMoment !== bMoment) return aMoment ? -1 : 1;
  const am = msOr(aMoment ? a.momentEndsAt : a.decidesBy, a.startsAt);
  const bm = msOr(bMoment ? b.momentEndsAt : b.decidesBy, b.startsAt);
  return am - bm;
}

// AFTER
export function compareActions(a: Timed, b: Timed): number {
  const aMoment = a.phase === "moment";
  const bMoment = b.phase === "moment";
  if (aMoment !== bMoment) return aMoment ? -1 : 1;
  const am = msOr(activeDeadline(a).iso, a.startsAt);
  const bm = msOr(activeDeadline(b).iso, b.startsAt);
  return am - bm;
}
// activeDeadline(moment) -> momentEndsAt, activeDeadline(collecting) -> decidesBy:
// identical to the inline ternary for the only two phases compareActions ever receives.
```

### Files
- `apps/mobile/src/lib/status.ts`

---

## 20. Extract a one-shot route-param consume hook in GroupDetail

`extract-consume-once-param-hook-groupdetail` - **effort** S | **risk** low | **impact** low

### Problem
GroupDetail's justCreated (lines 106-112) and justJoined (lines 115-121) effects are the same five-step skeleton: a module-level useRef(false) guard, gate on `!ref.current && route.params?.flag && data`, set the ref, run one side effect, then clear the param via navigation.setParams({ flag: undefined }). The duplicated guard-ref + clear-param boilerplate, plus the two bookkeeping refs handledCreate (68) and handledJoin (69), collapses into one tiny local hook (useConsumeParam) owning its own useRef(false), called twice. Behavior is identical: fires once, only after data loads, then clears the param. Keep the hook local to GroupDetail.tsx - JoinGroup's autoRan (JoinGroup.tsx:65) and Account's seeded (Account.tsx:43) are similar guard refs but neither clears a route param via setParams, so a shared abstraction would be a worse fit.

### Why it matters
Removes duplicated guard-ref + clear-param boilerplate and two module-level bookkeeping refs, collapsing two ~7-line effects into one reusable local hook called twice. Pure readability/dedup cleanup; identical timing and behavior (fires once, only after data loads, clears the param).

### Change steps
- Add a small local hook at top of GroupDetail.tsx (above the component): `function useConsumeParam(active: boolean, onConsume: () => void)` that owns its own `useRef(false)`, and in a `useEffect` keyed on [active] runs `onConsume()` exactly once when `active` first becomes true (guard: `if (active && !done.current) { done.current = true; onConsume(); }`).
- Delete the two module-level refs `handledCreate` (line 68) and `handledJoin` (line 69).
- Delete the justCreated useEffect block (lines 106-112) and replace with one call: `useConsumeParam(!!route.params?.justCreated && !!data, () => { setInviteOpen(true); navigation.setParams({ justCreated: undefined }); });`
- Delete the justJoined useEffect block (lines 115-121) and replace with one call: `useConsumeParam(!!route.params?.justJoined && !!data, () => { setWelcome(true); navigation.setParams({ justJoined: undefined }); });`
- Leave seededName (line 67), JoinGroup's autoRan, and Account's seeded untouched - different shapes (no setParams clear).
- Verify `useRef`/`useEffect` imports still resolve (both already imported on line 2; if no other `useEffect` user remained you would prune it, but the inviteOpen effect at lines 100-102 still uses it - keep both imports).
- Run pnpm lint, pnpm typecheck, pnpm test.

### Before / after
```ts
BEFORE (GroupDetail.tsx):
  const seededName = useRef(false);
  const handledCreate = useRef(false);   // line 68
  const handledJoin = useRef(false);     // line 69
  ...
  // Land-on-invite after creating the group...
  useEffect(() => {                       // lines 106-112
    if (!handledCreate.current && route.params?.justCreated && data) {
      handledCreate.current = true;
      setInviteOpen(true);
      navigation.setParams({ justCreated: undefined });
    }
  }, [route.params?.justCreated, data, navigation]);

  // One-time welcome band after joining via an invite.
  useEffect(() => {                       // lines 115-121
    if (!handledJoin.current && route.params?.justJoined && data) {
      handledJoin.current = true;
      setWelcome(true);
      navigation.setParams({ justJoined: undefined });
    }
  }, [route.params?.justJoined, data, navigation]);

AFTER:
  // Run a side effect exactly once, the first time `active` becomes true (e.g. a one-shot
  // route param that should fire only after data has loaded, then be cleared).
  function useConsumeParam(active: boolean, onConsume: () => void) {
    const done = useRef(false);
    useEffect(() => {
      if (active && !done.current) {
        done.current = true;
        onConsume();
      }
    }, [active, onConsume]);
  }

  // ...inside GroupDetail, replacing the two effects:
  const seededName = useRef(false);       // unchanged; handledCreate/handledJoin deleted
  ...
  // Land-on-invite after creating the group: open the sheet once loaded, then clear the flag.
  useConsumeParam(!!route.params?.justCreated && !!data, () => {
    setInviteOpen(true);
    navigation.setParams({ justCreated: undefined });
  });
  // One-time welcome band after joining via an invite.
  useConsumeParam(!!route.params?.justJoined && !!data, () => {
    setWelcome(true);
    navigation.setParams({ justJoined: undefined });
  });

Note: pass a stable `onConsume`. The inline arrows above are recreated each render, but the `done.current` guard makes that harmless (effect only acts once); to silence exhaustive-deps cleanly, the hook depends on [active, onConsume] and the once-guard is the real gate. If lint flags the inline closures, wrap each in useCallback or keep the dep array as [active] with a biome-ignore - match whatever the existing inviteOpen effect (lines 100-102) does for consistency.
```

### Files
- `apps/mobile/src/screens/GroupDetail.tsx`

---

## 21. Extract a shared bordered input-frame style constant for Field, DateTimeField, and DateTimePill

`extract-fieldbox-style-constant` - **effort** S | **risk** low | **impact** low

### Problem
The bordered "input surface box" recipe - the four frame properties `{ backgroundColor: ui.surface, borderWidth: ui.border, borderColor: ui.ink, borderRadius: ui.rInput }` - is hand-typed verbatim in three ui siblings: Field.tsx (box L43-46), DateTimeField.tsx non-bare branch (L136-139), and DateTimePill.tsx (box L35-38). DateTimeField's own comment says its trigger "mirrors the `Field` look", confirming the duplication is intentional shared styling, not a coincidence. All four values are already theme tokens and are byte-identical across the three sites. Add ONE style-only constant `fieldBox` to theme.ts holding exactly those four properties, and spread it at each call site, layering the per-site bits (flexDirection/alignItems/padding/overflow) inline unchanged on top. This single-sources the frame so the input look changes in one place, with zero rendered-style change because the spread reproduces the same values. Scope is deliberately the style-constant form only: NOT an InputShell/InputFrame component that wraps HardShadow + a row (the three sites diverge on layout direction, alignment, padding, and overflow, so a wrapper would need per-site overrides and invites drift). EXCLUDE the web DateTimeField mirror (CSS shorthand, cannot share an RN style object) and EXCLUDE any "align DateTimeField padding to Field" tweak (that changes rendered padding, so it is not behavior-preserving). Each site keeps its own HardShadow wrapper and style-prop threading.

### Why it matters
The bordered input frame is now defined once; future tweaks to the input surface/border/radius land in a single token instead of three (soon four, counting any new bordered input) drifting copies. The DateTimeField comment explicitly states its trigger "mirrors the Field look", so the shared constant encodes a relationship the code already intends. Risk is minimal because the spread reproduces identical token values, so no pixel changes; typecheck plus the existing DateTimeField tests prove behavior is preserved.

### Change steps
- In apps/mobile/src/theme.ts, add a style-only constant exporting the four shared frame properties, e.g. `export const fieldBox = { backgroundColor: ui.surface, borderWidth: ui.border, borderColor: ui.ink, borderRadius: ui.rInput } as const;` placed after the `ui` object (so it can reference the tokens). Keep it a plain object literal; do not pull in StyleSheet.create or RN types.
- In apps/mobile/src/ui/Field.tsx, import `fieldBox` from ../theme and replace the four inlined frame lines on the box View (L43-46) with `...fieldBox`, keeping `flexDirection: "row", alignItems: "center", paddingHorizontal: 11` inline and the inner TextInput's `paddingVertical: 10` unchanged. Leave the `HardShadow radius={ui.rInput} offset={ui.shadowInput}` wrapper untouched.
- In apps/mobile/src/ui/DateTimeField.tsx, import `fieldBox` from ../theme and in the non-bare branch of the `trigger` Pressable style (L133-142) replace the four frame lines (L136-139) with `...fieldBox`, keeping `flexDirection: "row", alignItems: "center", paddingHorizontal: 11, paddingVertical: 11` inline. Do NOT touch the `bare` branch (L127-132) - it intentionally has no frame. Leave the HardShadow wrapper at L162 untouched.
- In apps/mobile/src/ui/DateTimePill.tsx, import `fieldBox` from ../theme and replace the four frame lines on the box View (L35-38) with `...fieldBox`, keeping `flexDirection: "row", alignItems: "stretch", overflow: "hidden"` inline and the zero padding (i.e. add no padding). Leave the `HardShadow ... style={style}` wrapper untouched.
- Run `pnpm typecheck` and the DateTimeField tests (`pnpm test`) to confirm the rendered styles are byte-identical and types still resolve. Run `pnpm lint` (biome) and auto-fix import ordering if needed.

### Before / after
```ts
// theme.ts (new, after the `ui` object)
export const fieldBox = {
  backgroundColor: ui.surface,
  borderWidth: ui.border,
  borderColor: ui.ink,
  borderRadius: ui.rInput,
} as const;

// Field.tsx  (box View, L39-48)  BEFORE
<View style={{
  flexDirection: "row", alignItems: "center",
  backgroundColor: ui.surface, borderWidth: ui.border,
  borderColor: ui.ink, borderRadius: ui.rInput,
  paddingHorizontal: 11,
}}>
// AFTER
<View style={{
  ...fieldBox,
  flexDirection: "row", alignItems: "center",
  paddingHorizontal: 11,
}}>

// DateTimeField.tsx  (non-bare branch, L133-142)  BEFORE
: {
    flexDirection: "row", alignItems: "center",
    backgroundColor: ui.surface, borderWidth: ui.border,
    borderColor: ui.ink, borderRadius: ui.rInput,
    paddingHorizontal: 11, paddingVertical: 11,
  }
// AFTER
: {
    ...fieldBox,
    flexDirection: "row", alignItems: "center",
    paddingHorizontal: 11, paddingVertical: 11,
  }
// (bare branch L127-132 unchanged - no frame)

// DateTimePill.tsx  (box View, L31-40)  BEFORE
<View style={{
  flexDirection: "row", alignItems: "stretch",
  backgroundColor: ui.surface, borderWidth: ui.border,
  borderColor: ui.ink, borderRadius: ui.rInput,
  overflow: "hidden",
}}>
// AFTER
<View style={{
  ...fieldBox,
  flexDirection: "row", alignItems: "stretch",
  overflow: "hidden",
}}>

// All three keep their own <HardShadow radius={ui.rInput} offset={ui.shadowInput}> wrapper.
```

### Files
- `apps/mobile/src/theme.ts`
- `apps/mobile/src/ui/Field.tsx`
- `apps/mobile/src/ui/DateTimeField.tsx`
- `apps/mobile/src/ui/DateTimePill.tsx`

---

## 22. De-duplicate DateTimeField's mode-gated picker props across Android/iOS branches

`dedup-datetimefield-mode-gated-picker-props` - **effort** S | **risk** low | **impact** low

### Problem
In apps/mobile/src/ui/DateTimeField.tsx the three mode-gated native-picker props - `minuteInterval` (time mode only), `minimumDate` and `maximumDate` (date mode only) - are written out verbatim at two separate call sites: the imperative DateTimePickerAndroid.open() object (lines 109-111) and the iOS <DateTimePicker> JSX (lines 186-188). On top of that, `mode === "date"` / `mode === "time"` is spelled inline ~11 times in the component. The web sibling DateTimeField.web.tsx already derives `const isDate = mode === "date"` once (line 24) and computes its constrained `min`/`max`/`step` from it. Mirror that pattern in the native file: compute `isDate` and the three gated values once in the component body, then reference them at both sites. Purely behavior-preserving - the resolved props are byte-for-byte identical; it just removes the duplicated triple and aligns the native file with the web variant's `isDate` convention.

### Why it matters
The mode gating for the native picker's constrained props is currently a literal triple copied across the Android and iOS branches, plus ~11 inline `mode === "date"`/`"time"` spellings - so a future change to how a bound is gated (or the addition of a fourth gated prop) has to be made and kept in sync in two places, and the native file is needlessly out of step with its own web sibling, which already centralises this as `isDate`. Computing `isDate` and the three gated values once removes the duplication, gives a single source for the per-mode gating, and makes the two platform branches read as "pass the same constrained value" rather than re-deriving it. Low impact (cosmetic, one small file) but trivially safe and high-confidence, so it ranks first in this cluster.

### Change steps
- Add `const isDate = mode === "date";` near the top of the DateTimeField component body in apps/mobile/src/ui/DateTimeField.tsx (after the existing useState lines, before/with `shown`/`placeholder`), matching the web sibling's line-24 convention.
- Derive the three gated picker values once just below it: `const gatedMin = isDate ? minimumDate : undefined;`, `const gatedMax = isDate ? maximumDate : undefined;`, `const gatedInterval = isDate ? undefined : minuteInterval;`.
- In the Android DateTimePickerAndroid.open() object (lines 109-111), replace `minuteInterval: mode === "time" ? minuteInterval : undefined` with `minuteInterval: gatedInterval`, `minimumDate: mode === "date" ? minimumDate : undefined` with `minimumDate: gatedMin`, and `maximumDate: mode === "date" ? maximumDate : undefined` with `maximumDate: gatedMax`.
- In the iOS <DateTimePicker> props (lines 186-188), replace the matching three `mode === ...` ternaries with `minimumDate={gatedMin}`, `maximumDate={gatedMax}`, and `minuteInterval={gatedInterval}`.
- Optional consistency pass: reuse `isDate` in the two `display` ternaries (line 112 `display: isDate ? "calendar" : "clock"` and line 182 `display={isDate ? "inline" : "spinner"}`) and in the line-95 `placeholder` ternary; leave it if it muddies the diff.
- Do NOT collapse the two sites into a single shared spread object - one is an imperative plain object, the other is JSX, so a shared literal does not fit cleanly. Keep the three named constants and pass them into each site.
- Do NOT touch the `mode === "date"` checks inside seed() (lines 58, 60) or displayValue() (line 69) - those helpers receive `mode` as a parameter and live outside the component scope, so `isDate` is not in scope there.
- Run `pnpm typecheck` and `pnpm lint` to confirm no behavior or type drift.

### Before / after
```ts
// BEFORE - apps/mobile/src/ui/DateTimeField.tsx (component body)
const shown = displayValue(mode, value);
const placeholder = mode === "date" ? "Pick a date" : "Pick a time";
...
// Android (lines 109-111)
DateTimePickerAndroid.open({
  value: initial,
  mode,
  is24Hour: false,
  minuteInterval: mode === "time" ? minuteInterval : undefined,
  minimumDate: mode === "date" ? minimumDate : undefined,
  maximumDate: mode === "date" ? maximumDate : undefined,
  display: mode === "date" ? "calendar" : "clock",
  ...
});
...
// iOS (lines 186-188)
<DateTimePicker
  ...
  display={mode === "date" ? "inline" : "spinner"}
  minimumDate={mode === "date" ? minimumDate : undefined}
  maximumDate={mode === "date" ? maximumDate : undefined}
  minuteInterval={mode === "time" ? minuteInterval : undefined}
  ...
/>

// AFTER (mirrors DateTimeField.web.tsx's `isDate` on line 24)
const isDate = mode === "date";
const gatedMin = isDate ? minimumDate : undefined;
const gatedMax = isDate ? maximumDate : undefined;
const gatedInterval = isDate ? undefined : minuteInterval;

const shown = displayValue(mode, value);
const placeholder = isDate ? "Pick a date" : "Pick a time";
...
// Android
DateTimePickerAndroid.open({
  value: initial,
  mode,
  is24Hour: false,
  minuteInterval: gatedInterval,
  minimumDate: gatedMin,
  maximumDate: gatedMax,
  display: isDate ? "calendar" : "clock",
  ...
});
...
// iOS
<DateTimePicker
  ...
  display={isDate ? "inline" : "spinner"}
  minimumDate={gatedMin}
  maximumDate={gatedMax}
  minuteInterval={gatedInterval}
  ...
/>
```

### Files
- `apps/mobile/src/ui/DateTimeField.tsx`

---

## 23. Add a one-letter avatar-initial helper for person avatars

`mobile-first-initial-helper` - **effort** S | **risk** low | **impact** low

### Problem
The single-letter person-avatar initial is computed inline with `name.charAt(0).toUpperCase()` at all three person `<Avatar>` call sites (PersonRow.tsx:44, Account.tsx:98, AccountAvatar.tsx:9). This idiom is deliberately distinct from the existing two-letter `initials()` helper (used for GROUP avatars at GroupsList.tsx:100 and JoinGroup.tsx:149), so `initials()` cannot be reused without changing output. Extract a tiny `firstInitial(name)` helper next to `initials()` in apps/mobile/src/lib/format.ts and route the three person sites through it. The `Avatar` component keeps its presentation-only `initial: string` prop (do NOT move derivation into Avatar). Output is byte-for-byte identical, including the empty-string case. Pure mobile-internal extraction; no shared-package or runtime-import concern.

### Why it matters
Removes a re-inlined idiom duplicated across three files and gives the person-initial rule one named home next to the related `initials`/`colorFor` avatar helpers, making the deliberate one-letter vs two-letter distinction explicit. Behavior-preserving with zero output change.

### Change steps
- Add `export function firstInitial(name: string): string { return name.charAt(0).toUpperCase(); }` directly beneath the existing `initials()` (after line 82) in apps/mobile/src/lib/format.ts, beside `colorFor`/`initials`.
- In apps/mobile/src/ui/PersonRow.tsx: add `import { firstInitial } from "../lib/format";` and change line 44 `initial={name.charAt(0).toUpperCase()}` to `initial={firstInitial(name)}`.
- In apps/mobile/src/screens/Account.tsx: add `firstInitial` to the imports from `../lib/format` (file does not yet import from format, so add a new import line) and change line 98 `initial={shownName.charAt(0).toUpperCase()}` to `initial={firstInitial(shownName)}`.
- In apps/mobile/src/components/AccountAvatar.tsx: add `import { firstInitial } from "../lib/format";` and change line 9 `initial={name.charAt(0).toUpperCase()}` to `initial={firstInitial(name)}`.
- Do NOT touch the two group sites GroupsList.tsx:100 and JoinGroup.tsx:149 (they pass two-letter `initials(...)`), do NOT change the Avatar component or its `initial` prop, and do NOT touch format.ts:129 `partOfDayLabel` (it titlecases a full word with `.slice(1)`, not a person initial).
- Run `pnpm lint`, `pnpm typecheck`, and `pnpm test` to confirm no behavior or type changes.

### Before / after
```ts
// apps/mobile/src/lib/format.ts (beside the existing two-letter `initials`)
export function initials(name: string): string {
  const parts = name.trim().split(/\s+/);
  return ((parts[0]?.[0] ?? "") + (parts[1]?.[0] ?? "")).toUpperCase();
}
// AFTER - add:
export function firstInitial(name: string): string {
  return name.charAt(0).toUpperCase();
}

// PersonRow.tsx:44
// BEFORE: <Avatar initial={name.charAt(0).toUpperCase()} color={color} size={avatarSize} />
// AFTER:  <Avatar initial={firstInitial(name)} color={color} size={avatarSize} />

// Account.tsx:98
// BEFORE: <Avatar initial={shownName.charAt(0).toUpperCase()} color={ui.brand} size={44} />
// AFTER:  <Avatar initial={firstInitial(shownName)} color={ui.brand} size={44} />

// AccountAvatar.tsx:9
// BEFORE: return <Avatar initial={name.charAt(0).toUpperCase()} color={ui.brand} size={28} />;
// AFTER:  return <Avatar initial={firstInitial(name)} color={ui.brand} size={28} />;
```

### Files
- `apps/mobile/src/lib/format.ts`
- `apps/mobile/src/ui/PersonRow.tsx`
- `apps/mobile/src/screens/Account.tsx`
- `apps/mobile/src/components/AccountAvatar.tsx`

---

## 24. Replace the inlined display-font-15 heading recipe with an AppText variant

`mobile-apptext-cardtitle-variant` - **effort** S | **risk** low | **impact** low

### Problem
The raw heading recipe font.display/15/ui.ink is inlined byte-for-byte at GroupsList.tsx:102 (group-name row) and EventDetail.tsx:550 (fizzled-card title). Both bypass the AppText typographic vocabulary and sit 1px off the existing title variant (15 vs 16). Behavior-preserving fix: add a single cardTitle variant (display/15/ink) to apps/mobile/src/ui/Text.tsx and route both raw Text sites through AppText variant cardTitle, retiring the duplicated inline style with zero visual change.

### Why it matters
Both call sites re-type a recipe AppText was built to own and drift 1px from the nearest variant. One cardTitle variant gives the recipe a single home, removes the byte-identical duplication, and keeps output pixel-identical (display/15/ink unchanged), so no user-visible change.

### Change steps
- In apps/mobile/src/ui/Text.tsx, add cardTitle to the Variant union type (line 7).
- In apps/mobile/src/ui/Text.tsx, add cardTitle: { fontFamily: font.display, fontSize: 15, color: ui.ink } to the V record after the title entry (~line 11).
- In apps/mobile/src/screens/GroupsList.tsx, replace the raw Text at lines 102-104 with AppText variant cardTitle wrapping g.name.
- In apps/mobile/src/screens/EventDetail.tsx, replace the raw Text at lines 550-552 with AppText variant cardTitle wrapping DIDNT_COME_TOGETHER.
- Leave the font/ui imports in both screens intact - still used elsewhere (GroupsList chevron at :110; EventDetail ui.brand/ui.ink).
- Run pnpm lint, pnpm typecheck, and pnpm test before opening the PR.

### Before / after
```ts
Text.tsx: add cardTitle to Variant union and add V entry cardTitle: { fontFamily: font.display, fontSize: 15, color: ui.ink }. GroupsList.tsx:102-104 replace raw Text style font.display/15/ink wrapping g.name with AppText variant cardTitle. EventDetail.tsx:550-552 replace raw Text style font.display/15/ink wrapping DIDNT_COME_TOGETHER with AppText variant cardTitle.
```

### Files
- `apps/mobile/src/ui/Text.tsx`
- `apps/mobile/src/screens/GroupsList.tsx`
- `apps/mobile/src/screens/EventDetail.tsx`

---

## 25. Add a paragraph-caption variant instead of the scattered lineHeight:18 override

`add-captionpara-variant` - **effort** S | **risk** low | **impact** low

### Problem
AppText's `caption` variant sets `lineHeight: 16` (tight, for single-line labels), but every paragraph-style body caption re-overrides it back to `lineHeight: 18` inline. That override is repeated at 7 sites across mobile (Dashboard, EventDetail x3, GroupsList, JoinGroup, and the shared Section component). The tight-vs-paragraph caption distinction therefore exists only as a per-call-site magic number, not as a named, reusable variant. Promote it: add a `captionPara` variant (font.medium / fontSize 11 / color ui.muted / lineHeight 18) to the V record in Text.tsx and switch the 7 paragraph sites to it, dropping only the inline `lineHeight: 18` while keeping their marginTop/marginBottom/color props. Behavior-identical (same rendered line-height) with one source of truth. The 3 caption sites that override to `lineHeight: 16` (Account.tsx:117, CreateWizard.tsx:575, EventDetail.tsx:414) are intentionally out of scope.

### Why it matters
The paragraph-vs-tight caption distinction is real design intent but lives only as a copy-pasted lineHeight:18 magic number at 7 call sites (one of them the shared Section component used app-wide). Promoting it to a named variant gives the design system one source of truth, lets future paragraph captions pick the right line-height by name instead of re-deriving 18, and shrinks each call site. It mirrors the existing pattern of every recurring font+size+color recipe being a named variant in Text.tsx.

### Change steps
- Add a `captionPara` entry to the `V` record in apps/mobile/src/ui/Text.tsx ({ fontFamily: font.medium, fontSize: 11, color: ui.muted, lineHeight: 18 }) and extend the `Variant` union type to include "captionPara".
- Dashboard.tsx:299 - change `variant="caption" style={{ marginTop: 6, lineHeight: 18 }}` to `variant="captionPara" style={{ marginTop: 6 }}`.
- EventDetail.tsx:508 - change `variant="caption" style={{ marginTop: 8, lineHeight: 18 }}` to `variant="captionPara" style={{ marginTop: 8 }}`.
- EventDetail.tsx:553 - change `variant="caption" style={{ marginTop: 6, lineHeight: 18 }}` to `variant="captionPara" style={{ marginTop: 6 }}`.
- EventDetail.tsx:840 - change `variant="caption" style={{ lineHeight: 18 }}` to `variant="captionPara"` (drop the now-empty style prop entirely).
- GroupsList.tsx:121 - change `variant="caption" style={{ marginTop: 6, lineHeight: 18 }}` to `variant="captionPara" style={{ marginTop: 6 }}`.
- JoinGroup.tsx:128 - change `variant="caption" style={{ marginTop: 6, lineHeight: 18 }}` to `variant="captionPara" style={{ marginTop: 6 }}`.
- Section.tsx:39 - change `variant="caption" style={{ marginTop: 6, marginBottom: lg ? 14 : 10, lineHeight: 18 }}` to `variant="captionPara" style={{ marginTop: 6, marginBottom: lg ? 14 : 10 }}`.
- Run `pnpm lint`, `pnpm typecheck`, `pnpm test` to confirm no behavior or type change.

### Before / after
```ts
// apps/mobile/src/ui/Text.tsx - V record

// BEFORE
type Variant = "screenTitle" | "title" | "rowLabel" | "rowLabelSm" | "body" | "caption";
const V: Record<Variant, TextStyle> = {
  ...
  caption: { fontFamily: font.medium, fontSize: 11, color: ui.muted, lineHeight: 16 },
};

// AFTER
type Variant =
  | "screenTitle" | "title" | "rowLabel" | "rowLabelSm" | "body" | "caption" | "captionPara";
const V: Record<Variant, TextStyle> = {
  ...
  // Tight single-line caption (labels, hints).
  caption: { fontFamily: font.medium, fontSize: 11, color: ui.muted, lineHeight: 16 },
  // Same caption, looser line-height for multi-line paragraph body copy.
  captionPara: { fontFamily: font.medium, fontSize: 11, color: ui.muted, lineHeight: 18 },
};

// ---- call sites (representative) ----

// Dashboard.tsx:299  BEFORE
<AppText variant="caption" style={{ marginTop: 6, lineHeight: 18 }}>
// AFTER
<AppText variant="captionPara" style={{ marginTop: 6 }}>

// EventDetail.tsx:840  BEFORE  (only style prop is the lineHeight - drop it)
<AppText variant="caption" style={{ lineHeight: 18 }}>
// AFTER
<AppText variant="captionPara">

// Section.tsx:39  BEFORE  (keep the dynamic marginBottom)
<AppText variant="caption" style={{ marginTop: 6, marginBottom: lg ? 14 : 10, lineHeight: 18 }}>
// AFTER
<AppText variant="captionPara" style={{ marginTop: 6, marginBottom: lg ? 14 : 10 }}>
```

### Files
- `apps/mobile/src/ui/Text.tsx`
- `apps/mobile/src/screens/Dashboard.tsx`
- `apps/mobile/src/screens/EventDetail.tsx`
- `apps/mobile/src/screens/GroupsList.tsx`
- `apps/mobile/src/screens/JoinGroup.tsx`
- `apps/mobile/src/ui/Section.tsx`

---

## 26. Extract the tap-to-Account avatar header slot duplicated in GroupsList and Dashboard

`extract-account-header-button` - **effort** S | **risk** low | **impact** low

### Problem
The ScreenHeader `right` slot - `<Pressable onPress={() => navigation.navigate("Account")} hitSlop={8}><AccountAvatar /></Pressable>` - is byte-identical in GroupsList.tsx:82-84 and Dashboard.tsx:259-261. Each screen re-creates the same pressable wrapper around the already-shared AccountAvatar. Extract the wrapper as a single reusable `AccountHeaderButton({ onPress })` component, render it as the header `right` in both screens, and drop the now-unused `AccountAvatar` and `Pressable` imports from both. Behavior-preserving dedup only.

### Why it matters
Removes a byte-identical pressable wrapper duplicated across two screens, giving the account-header affordance one home so a future change (hitSlop, accessibility label, a11y role) is made once instead of twice, and clears two now-dead imports per screen.

### Change steps
- Create apps/mobile/src/components/AccountHeaderButton.tsx exporting `AccountHeaderButton({ onPress }: { onPress: () => void })` that renders `<Pressable onPress={onPress} hitSlop={8}><AccountAvatar /></Pressable>`; move the AccountAvatar import into this file (import Pressable from react-native, AccountAvatar from ./AccountAvatar).
- In GroupsList.tsx, replace the inline wrapper (lines 82-84) with `<AccountHeaderButton onPress={() => navigation.navigate("Account")} />`; add the AccountHeaderButton import; remove the now-unused AccountAvatar import (line 6) and drop Pressable from the react-native import (line 4, leaving Text, View).
- In Dashboard.tsx, replace the inline wrapper (lines 259-261) with `<AccountHeaderButton onPress={() => navigation.navigate("Account")} />`; add the AccountHeaderButton import; remove the now-unused AccountAvatar import (line 7) and drop Pressable from the react-native import (line 5).
- Run pnpm lint, pnpm typecheck, pnpm test to confirm no behavior change and no dangling imports.

### Before / after
```ts
New file - apps/mobile/src/components/AccountHeaderButton.tsx:

  import { Pressable } from "react-native";
  import { AccountAvatar } from "./AccountAvatar";

  // Header right-slot button: the signed-in user's avatar, tapping it opens the Account tab.
  // Takes an onPress instead of a navigation prop so it stays decoupled from any stack's param list.
  export function AccountHeaderButton({ onPress }: { onPress: () => void }) {
    return (
      <Pressable onPress={onPress} hitSlop={8}>
        <AccountAvatar />
      </Pressable>
    );
  }

GroupsList.tsx (and identically Dashboard.tsx):

  - import { Pressable, Text, View } from "react-native";
  + import { Text, View } from "react-native";
  - import { AccountAvatar } from "../components/AccountAvatar";
  + import { AccountHeaderButton } from "../components/AccountHeaderButton";

    right={
  -   <Pressable onPress={() => navigation.navigate("Account")} hitSlop={8}>
  -     <AccountAvatar />
  -   </Pressable>
  +   <AccountHeaderButton onPress={() => navigation.navigate("Account")} />
    }
```

### Files
- `apps/mobile/src/components/AccountHeaderButton.tsx`
- `apps/mobile/src/screens/GroupsList.tsx`
- `apps/mobile/src/screens/Dashboard.tsx`

---

## 27. Have GroupsList use the shared useFetchOnFocus hook

`groupslist-use-fetch-on-focus` - **effort** S | **risk** med | **impact** low

### Problem
GroupsList.tsx (lines 46-63) hand-rolls a useFocusEffect+useCallback with a belt-and-braces `active` flag to fetch groups.mine on focus. Its three siblings - Account, JoinGroup, GroupDetail - already use the shared useFetchOnFocus hook, whose doc deliberately omits the active guard for exactly this plain non-polling fetch-on-focus case. Switch GroupsList to the hook to match the siblings and delete the duplicated lifecycle. Behavior-preserving cleanup only.

### Why it matters
GroupsList is the lone holdout re-implementing the fetch-once-on-focus lifecycle that useFetchOnFocus exists for; the other three group/account screens already use the hook. Adopting it deletes ~16 lines of duplicated lifecycle and makes all four screens consistent, so the shared hook is the single place this pattern lives.

### Change steps
- In GroupsList.tsx, replace the inline useFocusEffect(useCallback(() => { let active = true; ... return () => { active = false }; }, [])) block (lines 46-63) with useFetchOnFocus(useCallback(() => { trpc.groups.mine.query().then((g) => { setGroups(g); setError(false); }).catch(() => setError(true)).finally(() => setLoading(false)); }, []))
- Add import { useFetchOnFocus } from "../lib/useFetchOnFocus" (mirrors GroupDetail.tsx line 26)
- Remove the now-unused `import { useFocusEffect } from "@react-navigation/native"` (line 1); useFocusEffect is no longer referenced in GroupsList
- Keep the useCallback import from "react" (still used to memoize the load body); keep useState
- Run pnpm lint, pnpm typecheck, and pnpm test (Groups.test.tsx covers this screen) to confirm parity

### Before / after
```ts
// GroupsList.tsx

// --- before ---
import { useFocusEffect } from "@react-navigation/native";
import { useCallback, useState } from "react";
// ...
useFocusEffect(
  useCallback(() => {
    let active = true;
    trpc.groups.mine
      .query()
      .then((g) => {
        if (active) {
          setGroups(g);
          setError(false);
        }
      })
      .catch(() => active && setError(true))
      .finally(() => active && setLoading(false));
    return () => {
      active = false;
    };
  }, []),
);

// --- after ---
import { useCallback, useState } from "react";
import { useFetchOnFocus } from "../lib/useFetchOnFocus";
// ...
useFetchOnFocus(
  useCallback(() => {
    trpc.groups.mine
      .query()
      .then((g) => {
        setGroups(g);
        setError(false);
      })
      .catch(() => setError(true))
      .finally(() => setLoading(false));
  }, []),
);
// (drop the now-unused useFocusEffect import from @react-navigation/native)
```

### Files
- `apps/mobile/src/screens/GroupsList.tsx`

---

## 28. Use the ui.gutter token instead of the hardcoded 16 in ScreenBackground

`screenbackground-header-gutter-token` - **effort** S | **risk** low | **impact** low

### Problem
ScreenBackground pins its header inset with a raw `paddingHorizontal: 16` (line 27), while its siblings ScreenScroll and Band derive the same screen-edge inset from `ui.gutter` (= 16, documented in theme.ts as "the screen edge gutter (scroll padding, header padding, full-bleed bleed)"). `ui` is already imported at line 5, so this is a single, value-identical token swap that gives the screen-edge gutter one source of truth and stops the header from silently drifting if the token ever changes. Verified: `ui.gutter === 16`, and ScreenBackground.tsx:27 is the only `paddingHorizontal: 16` literal in apps/mobile/src/ui/, so there are no further call sites in scope.

### Why it matters
Centralizes the screen-edge gutter so ScreenBackground's header inset stays aligned with ScreenScroll and Band; removes a magic number that would silently drift the header if the gutter token ever changes.

### Change steps
- In apps/mobile/src/ui/ScreenBackground.tsx, line 27, replace the literal `paddingHorizontal: 16` with `paddingHorizontal: ui.gutter` (the `ui` import on line 5 already covers this; no import change needed).
- Run pnpm lint, pnpm typecheck, and pnpm test to confirm the no-op substitution compiles and passes.

### Before / after
```ts
Before (apps/mobile/src/ui/ScreenBackground.tsx:27):
        {header ? <View style={{ paddingHorizontal: 16 }}>{header}</View> : null}

After:
        {header ? <View style={{ paddingHorizontal: ui.gutter }}>{header}</View> : null}

Context: `ui` is already imported at line 5 (`import { ui } from "../theme";`) and theme.ts:14 defines `gutter: 16`. No other change required.
```

### Files
- `apps/mobile/src/ui/ScreenBackground.tsx`

---

## 29. Collapse the dead dismissedAction branch in the native share path

`collapse-dead-dismissedaction-share` - **effort** S | **risk** low | **impact** low

### Problem
In apps/mobile/src/lib/share.ts the native share branch captures `const result = await Share.share(...)`, then checks `if (result.action === Share.dismissedAction) return { copied: false }` before falling through to an identical `return { copied: false }`. Both arms produce the same value, so the test, the `result` binding, and the only reference to `Share.dismissedAction` are all dead. The structure misleadingly mirrors the web AbortError handling above it, implying a distinction (cancel vs not) that does not exist on this path. The caller (GroupDetail.share at line 145-146) only flashes when `copied === true`, which the native branch produces solely via the catch fallback - so collapsing the two arms into a single non-capturing `await Share.share(...)` plus `return { copied: false }` is behavior-preserving. Verified: only call site is GroupDetail.tsx; no tests reference share.ts or dismissedAction.

### Why it matters
The `result` binding, the `dismissedAction` test, and the duplicate return are dead: both branches yield `{ copied: false }`. Removing them deletes the only `Share.dismissedAction` reference and stops the code from implying a cancel-vs-success distinction that the native sheet does not surface here, while the caller's `copied === true` gating is unaffected.

### Change steps
- Open apps/mobile/src/lib/share.ts and locate the native branch inside the try block (lines 38-41).
- Replace the three lines - `const result = await Share.share({ message: `${text} ${url}`, url });`, `if (result.action === Share.dismissedAction) return { copied: false };`, and the trailing `return { copied: false };` - with a single non-capturing `await Share.share({ message: `${text} ${url}`, url });` immediately followed by `return { copied: false };`.
- Keep the existing inline comment on line 38 (`// iOS honours `url`; Android only reads `message`...`) and the surrounding try/catch copy fallback (lines 42-45) untouched.
- Confirm `Share.dismissedAction` has no remaining references in the file (it was the only use).
- Run pnpm lint, pnpm typecheck, and pnpm test before opening a PR.

### Before / after
```ts
Before (apps/mobile/src/lib/share.ts, lines 37-45):

  try {
    // iOS honours `url`; Android only reads `message`, so the URL rides along in the message too.
    const result = await Share.share({ message: `${text} ${url}`, url });
    if (result.action === Share.dismissedAction) return { copied: false };
    return { copied: false };
  } catch {
    await copyToClipboard(url);
    return { copied: true };
  }

After:

  try {
    // iOS honours `url`; Android only reads `message`, so the URL rides along in the message too.
    await Share.share({ message: `${text} ${url}`, url });
    return { copied: false };
  } catch {
    await copyToClipboard(url);
    return { copied: true };
  }
```

### Files
- `apps/mobile/src/lib/share.ts`

---

## 30. Derive the wizard STEPS array from the StepKey union (single-source the step order)

`derive-wizard-steps-from-stepkey-union` - **effort** S | **risk** low | **impact** low

### Problem
redo.ts declares the canonical create-wizard step order twice: the `StepKey` union (lines 50-57) and a function-local `STEPS: StepKey[]` array inside `wizardSteps` (lines 60-68) re-list the same seven literals in the same order. This is an unenforced parallel pair - the compiler does not check the array against the union for completeness (only `STEP_COPY`'s `Record<StepKey, ...>` is exhaustiveness-checked). A literal could exist in the union and copy table yet be omitted from the sequence with no type error, and the array is needlessly rebuilt on every call. Lift one ordered list to module scope as the single source of truth and derive the `StepKey` type from it. Pure type/structure change: `wizardSteps(false)` and `wizardSteps(true)` return identical arrays in identical order, so CreateWizard, copy.ts, and redo.test.ts are unaffected.

### Why it matters
The order lives in two hand-maintained places that the compiler never cross-checks. They can silently drift: a step present in the union and STEP_COPY but dropped from the sequence (or reordered) compiles clean and ships a wrong wizard flow. Deriving the type from one ordered const makes the sequence the single source of truth, keeps StepKey exhaustive so STEP_COPY's Record<StepKey, ...> guard is unchanged, makes the order editable in exactly one place, and removes the per-call array rebuild.

### Change steps
- In apps/mobile/src/lib/redo.ts, add a module-scope const above the type: `const STEP_ORDER = ["group", "source", "activities", "times", "details", "deadlines", "confirm"] as const;`
- Replace the seven-line `StepKey` union (lines 50-57) with `export type StepKey = (typeof STEP_ORDER)[number];`
- Replace the body of `wizardSteps` (the function-local `STEPS` array, lines 60-68, plus the filter) with `return STEP_ORDER.filter((s) => s !== "source" || hasPast);`, keeping the signature `export function wizardSteps(hasPast: boolean): StepKey[]`.
- Run `pnpm typecheck` and `pnpm --filter @bethere/mobile test` (covers redo.test.ts) to confirm ordering assertions and the `Record<StepKey, ...>` exhaustiveness check still pass.

### Before / after
```ts
// BEFORE (apps/mobile/src/lib/redo.ts, lines 50-70)
export type StepKey =
  | "group"
  | "source"
  | "activities"
  | "times"
  | "details"
  | "deadlines"
  | "confirm";

export function wizardSteps(hasPast: boolean): StepKey[] {
  const STEPS: StepKey[] = [
    "group",
    "source",
    "activities",
    "times",
    "details",
    "deadlines",
    "confirm",
  ];
  return STEPS.filter((s) => s !== "source" || hasPast);
}

// AFTER
const STEP_ORDER = [
  "group",
  "source",
  "activities",
  "times",
  "details",
  "deadlines",
  "confirm",
] as const;

export type StepKey = (typeof STEP_ORDER)[number];

export function wizardSteps(hasPast: boolean): StepKey[] {
  return STEP_ORDER.filter((s) => s !== "source" || hasPast);
}
```

### Files
- `apps/mobile/src/lib/redo.ts`

---

## 31. Share the time-candidate start-extraction (filter + as-Date cast) across seed modules

`share-time-candidate-start-extraction-seed` - **effort** S | **risk** low | **impact** low

### Problem
seed.ts (lines 32-35) and seed-data.ts seedIntegrityErrors (lines 290-296) each re-implement the same "take a plan's TIME candidates that have a startsAt, then read their start times" logic, including the identical `(c.startsAt as Date)` cast that works around TS not narrowing `startsAt?: Date` after the `c.kind === "time" && c.startsAt` filter. seed.ts sorts to anchor a plan's startsAt/respondByAt on earliest/latest; seed-data.ts uses Math.min to validate decidesBy. Extract one exported pure helper `timeStarts(candidates: Cand[]): Date[]` in seed-data.ts (where Cand, candId, dayAt already live) that filters `kind === "time" && c.startsAt`, does the `as Date` narrowing once internally, sorts ascending, and returns the startsAt Dates. Both call sites then reuse it. Behavior is identical: seed.ts already sorted ascending so sorted[0]/sorted[last] are unchanged, and seed-data.ts's Math.min over the same set equals starts[0] of the ascending sort. Verified against the live code: Cand is exported (seed-data.ts:51), the two blocks match the description exactly, seed.ts already imports from ./seed-data.js (line 13), and the `sorted`/`timeCands` locals are used nowhere else in seed.ts (only lines 41 and 43). The optional second `timeStartMs` sub-item is dropped as redundant once timeStarts exists.

### Why it matters
Centralizes one duplicated predicate and the single justified `as Date` cast (currently written three times across two files) so the demo invariant "TIME candidates with a startsAt" lives once. Pure, behavior-preserving cleanup with no user-visible change and full test coverage (seed-data.test.ts) guarding it.

### Change steps
- Add to apps/api/src/db/seed-data.ts (near candId/dayAt, after the Cand interface): export function timeStarts(candidates: Cand[]): Date[] that filters c.kind === "time" && c.startsAt, maps to (c.startsAt as Date) (the single justified cast), sorts ascending by getTime(), and returns the Date[].
- In apps/api/src/db/seed-data.ts seedIntegrityErrors (lines 290-296): replace the inline filter+map+cast with `const starts = timeStarts(p.candidates);` then `if (p.decidesBy && starts.length > 0) { const earliest = starts[0].getTime(); if (p.decidesBy.getTime() > earliest) errors.push(`plan ${p.id}: decidesBy is after the earliest time candidate`); }` (sorted ascending, so starts[0] is the earliest, matching the old Math.min).
- In apps/api/src/db/seed.ts: add timeStarts to the existing import from "./seed-data.js" (line 13, keeping the .js ESM extension).
- In apps/api/src/db/seed.ts (lines 32-43): replace the `timeCands` filter and the `sorted` spread+sort (lines 32-35) with `const sorted = timeStarts(p.candidates);`, then change `sorted[0]?.startsAt` to `sorted[0]` (line 41) and `sorted[sorted.length - 1]?.startsAt` to `sorted[sorted.length - 1]` (line 43), since the helper now returns Dates; this drops both local `(... as Date)` casts.
- Run pnpm typecheck and pnpm test; confirm seed-data.test.ts stays green and the committed seed's integrity check still returns [].

### Before / after
```ts
// apps/api/src/db/seed-data.ts - new exported helper (near candId / dayAt)
// Returns the start times of a plan's TIME candidates, ascending.
// The single justified `as Date` cast lives here: TS does not narrow
// startsAt?: Date through the `kind === "time" && c.startsAt` filter.
export function timeStarts(candidates: Cand[]): Date[] {
  return candidates
    .filter((c) => c.kind === "time" && c.startsAt)
    .map((c) => c.startsAt as Date)
    .sort((a, b) => a.getTime() - b.getTime());
}

// apps/api/src/db/seed-data.ts - seedIntegrityErrors, BEFORE (290-296)
const startTimes = p.candidates
  .filter((c) => c.kind === "time" && c.startsAt)
  .map((c) => (c.startsAt as Date).getTime());
if (p.decidesBy && startTimes.length > 0) {
  const earliest = Math.min(...startTimes);
  if (p.decidesBy.getTime() > earliest)
    errors.push(`plan ${p.id}: decidesBy is after the earliest time candidate`);
}
// AFTER
const starts = timeStarts(p.candidates);
if (p.decidesBy && starts.length > 0) {
  const earliest = starts[0].getTime(); // sorted ascending => earliest
  if (p.decidesBy.getTime() > earliest)
    errors.push(`plan ${p.id}: decidesBy is after the earliest time candidate`);
}

// apps/api/src/db/seed.ts - import (line 13)
import { candId, DEMO_USERS, dayAt, GROUPS, PLANS, timeStarts } from "./seed-data.js";

// apps/api/src/db/seed.ts - BEFORE (32-43)
const timeCands = p.candidates.filter((c) => c.kind === "time" && c.startsAt);
const sorted = [...timeCands].sort(
  (a, b) => (a.startsAt as Date).getTime() - (b.startsAt as Date).getTime(),
);
const chosen = ...
const startsAt = chosen?.startsAt ?? sorted[0]?.startsAt ?? p.decidesBy ?? dayAt(2, 19);
const respondByAt =
  p.momentEndsAt ?? sorted[sorted.length - 1]?.startsAt ?? p.decidesBy ?? startsAt;
// AFTER
const sorted = timeStarts(p.candidates); // Date[]
const chosen = ...
const startsAt = chosen?.startsAt ?? sorted[0] ?? p.decidesBy ?? dayAt(2, 19);
const respondByAt =
  p.momentEndsAt ?? sorted[sorted.length - 1] ?? p.decidesBy ?? startsAt;
```

### Files
- `apps/api/src/db/seed-data.ts`
- `apps/api/src/db/seed.ts`

---

## 32. Share the onPress ? Pressable : View wrapper across Card, Row, PersonRow

`share-tappable-wrapper-card-row-personrow` - **effort** S | **risk** low | **impact** low

### Problem
Card.tsx (37-43), Row.tsx (28-34), and PersonRow.tsx (55-61) each hand-roll the identical idiom: `onPress ? <Pressable onPress [disabled] style>{children}</Pressable> : <View style>{children}</View>`. Extract one internal-to-mobile `Tappable` helper in apps/mobile/src/ui that renders a Pressable when `onPress` is set, else a plain View, with `style?: ViewStyle | ViewStyle[]` and an optional `disabled?: boolean`. Adopt it in all three components. Behavior-preserving only: Card keeps its `disabled` wiring and keeps its outer `style` prop on HardShadow (NOT routed through the helper); Row passes its single style object; PersonRow passes `[rowStyle, style]` and the `inner` fragment as children. No new props or feedback states (no pressed style, no accessibilityRole) since none exist today.

### Why it matters
The "make this surface optionally tappable" branch is re-implemented three times across the core surface primitives. A single `Tappable` wrapper removes the thrice-repeated `onPress ? Pressable : View` fork and gives future tap-feedback work (pressed styles, accessibilityRole) one place to land. Low individual value, but it is pure structural dedup with no behavior change.

### Change steps
- Create apps/mobile/src/ui/Tappable.tsx exporting `Tappable({ onPress, style, disabled, children })` with types `{ onPress?: () => void; style?: ViewStyle | ViewStyle[]; disabled?: boolean; children: ReactNode }`; render `onPress ? <Pressable onPress={onPress} disabled={disabled} style={style}>{children}</Pressable> : <View style={style}>{children}</View>`.
- In Card.tsx: import Tappable; replace the `onPress ? <Pressable.../> : <View.../>` block (lines 37-43) inside HardShadow with `<Tappable onPress={onPress} disabled={disabled} style={boxStyle}>{children}</Tappable>`. Leave HardShadow and its outer `style` prop untouched. Drop the now-unused `Pressable`/`View` imports if no longer referenced.
- In Row.tsx: import Tappable; replace the branch (lines 28-34) with `<Tappable onPress={onPress} style={style}>{children}</Tappable>`. Drop the now-unused `Pressable`/`View` imports.
- In PersonRow.tsx: import Tappable; replace the branch (lines 55-61) with `<Tappable onPress={onPress} style={[rowStyle, style]}>{inner}</Tappable>`. PersonRow still needs `View` (used at line 48 for the rightAlign wrapper) and `ViewStyle`, so keep those imports; drop only `Pressable`.
- Optionally add `export { Tappable } from "./Tappable";` to apps/mobile/src/ui/index.ts (consistent with the barrel), or leave it unexported as a private helper - either is fine since it is internal-only.
- Run pnpm lint, pnpm typecheck, and pnpm test to confirm no regressions.

### Before / after
```ts
// NEW: apps/mobile/src/ui/Tappable.tsx
import type { ReactNode } from "react";
import { Pressable, View, type ViewStyle } from "react-native";

// Renders a Pressable when `onPress` is set, otherwise a plain View - the "optionally
// tappable surface" idiom shared by Card, Row, and PersonRow.
export function Tappable({
  onPress,
  style,
  disabled,
  children,
}: {
  onPress?: () => void;
  style?: ViewStyle | ViewStyle[];
  disabled?: boolean;
  children: ReactNode;
}) {
  return onPress ? (
    <Pressable onPress={onPress} disabled={disabled} style={style}>
      {children}
    </Pressable>
  ) : (
    <View style={style}>{children}</View>
  );
}

// Card.tsx  (was lines 37-43, inside HardShadow)
- <HardShadow radius={radius} style={style}>
-   {onPress ? (
-     <Pressable onPress={onPress} disabled={disabled} style={boxStyle}>
-       {children}
-     </Pressable>
-   ) : (
-     <View style={boxStyle}>{children}</View>
-   )}
- </HardShadow>
+ <HardShadow radius={radius} style={style}>
+   <Tappable onPress={onPress} disabled={disabled} style={boxStyle}>
+     {children}
+   </Tappable>
+ </HardShadow>

// Row.tsx  (was lines 28-34)
- return onPress ? (
-   <Pressable onPress={onPress} style={style}>{children}</Pressable>
- ) : (
-   <View style={style}>{children}</View>
- );
+ return <Tappable onPress={onPress} style={style}>{children}</Tappable>;

// PersonRow.tsx  (was lines 55-61; note View still used at line 48)
- return onPress ? (
-   <Pressable onPress={onPress} style={[rowStyle, style]}>{inner}</Pressable>
- ) : (
-   <View style={[rowStyle, style]}>{inner}</View>
- );
+ return <Tappable onPress={onPress} style={[rowStyle, style]}>{inner}</Tappable>;
```

### Files
- `apps/mobile/src/ui/Tappable.tsx`
- `apps/mobile/src/ui/Card.tsx`
- `apps/mobile/src/ui/Row.tsx`
- `apps/mobile/src/ui/PersonRow.tsx`
- `apps/mobile/src/ui/index.ts`

---
